#경험 기록

## 2026-07-07

### 시작할 때 상태
어제 코딩 공부를 시작했고, 이틀 전에 Git을 설치했다. git add, git commit, git push라는 명령어 정도만 알고 있었다.

### 오늘 한 행동
master 브랜치를 main으로 변경한 일
git add와 git commit의 차이를 처음 구분한 일
mkdir를 mkidr로 잘못 입력하고 오류를 확인한 일
"문자열+정수+문자열"를 입력하고 오류를 확인한 일

### 막힌 문제
명령어의 개념을 명확히 하는 부분이 헷갈린다.
"문자열+정수+문자열"를 입력하고 오류가 난 것에서 해메었다.

### 해결과정
개념을 다시 종이에 필기해보았다.
문자열과 정수를 바로 더할 수 없다는 것을 확인해서, 정수를 str()로 바꾸는 법, 쉼표로 구분하는 법을 배웠고 f-string이라는 법도 있다는 것을 알게 되었다.
### 알게된 것
input으로 받은 값은 문자열이고, 계산하려면 int()로 바꿔야한다.
문자열과 정수는 +연산자로 바로 연결이 안 되므로 총 3가지의 방법으로 해결하는 것을 배웠다.
git add는 다음 commit에 포함할 변경 내용을 준비하고, git commit은 그 내용을 기록으로 남긴다.

### 다음 행동
내일 종이에 필기한 것을 다시 확인해본다.


## 2026-07-08 조건식 오류 수정

### 상황
시험 점수와 출석률을 각각 정수로 입력받고
int(), input(), if ,elif ,else ,and ,or을 사용하여
점수 또는 출석률이 0 미만이거나 100 초과이면
잘못된 입력입니다.
점수가 70 이상이고 출석률이 80 이상이면
수료입니다.
그 외에는
미수료입니다.
위 조건을 만족하는 수료 판정 프로그램을 작성했다.

### 문제
점수 105, 출석률 90을 입력했을 때
잘못된 입력이 아니라 수료라고 출력됐다.

### 내가 처음 시도한 방법
두 변수의 범위를 한꺼번에 검사하려고 (score or check_rate) < 0 and (score or check_rate) > 100을 사용했다.

### 잘못된 이유
`score or check_rate`는 두 변수를 모두 검사하는 표현이 아니었다.
두 값 중 참으로 취급되는 한 값을 반환하므로, 각 변수의 범위를 따로 비교하지 못했다.

또한 하나의 값이 동시에 0보다 작고 100보다 클 수 없으므로
`값 < 0 and 값 > 100` 조건은 항상 거짓이었다.

### 수정한 방법
score와 check_rate를 각각 비교하도록 조건식을 score < 0 or score > 100 or check_rate < 0 or check_rate > 100으로 수정함.
점수 85·출석률 105, 점수 85·출석률 -1을 입력해 다시 확인함.

### 결과
점수 85·출석률 105와 점수 85·출석률 -1을 입력했을 때
모두 `잘못된 입력입니다.`가 출력되는 것을 확인했다.
정상 범위의 입력에서는 수료와 미수료도 기준에 맞게 출력됐다.

### 이 경험에서 배운 점
여러 변수의 범위를 검사할 때는 변수마다 비교식을 따로 작성해야 한다.
코드가 실행된다는 것만으로 맞다고 판단하지 말고,
105, -1 같은 잘못된 입력과 경계값을 직접 넣어 확인해야 한다.


## 2026-07-09 Day 4 - 조건문을 배우면서 헷갈린 개념을 정리한 경험

### 상황
Python 조건문을 배우면서 if / elif / else, input, int, and 조건을 연습했다.  
처음에는 코드를 바꿔서 테스트해야 하는데, 기존 코드 아래에 계속 추가해서 실행 결과가 여러 번 나오는 실수를 했다.

### 문제
조건문 자체보다도 코드를 어떻게 테스트해야 하는지, 그리고 =와 ==의 차이를 구분하는 것이 헷갈렸다.  
또 input으로 받은 값이 바로 숫자가 아니라 문자열이라는 점도 처음에는 익숙하지 않았다.
if ticket == yes (x) if ticket == "yes" (o)

### 내가 한 행동
코드를 한 번에 계속 추가하지 않고, 기존 값을 바꿔가며 다시 실행하는 방식으로 고쳤다.  
점수 등급, 나이 판별, 짝수/홀수 판별, 티켓 입장 조건 문제를 직접 실행해 보면서 조건문 구조를 반복했다.  
또 공부일지에 =는 값 저장, ==는 비교, signed integer와 unsigned integer의 차이를 따로 정리했다.
비교 연산자에서 정의된 변수가 아닌 문자열에는 " "를 붙여주었다.

### 배운 점
조건문은 단순히 문법을 외우는 것이 아니라, 문제의 조건을 코드의 비교식으로 바꾸는 연습이 필요하다는 것을 알게 되었다.  
또 input은 문자열로 들어오기 때문에 숫자 비교를 하려면 int로 바꿔야 한다는 점을 알게 되었다.

### 자소서나 면접에 쓸 수 있는 포인트
기초 개념을 배울 때 실수를 그냥 넘기지 않고, 왜 틀렸는지 기록하고 다시 실행하면서 수정했다.  
처음에는 따라 치는 수준이었지만, 점점 문제 조건을 코드로 바꾸는 방식에 익숙해지고 있다.


## 2026-07-10 Day 5 - 반복문으로 점수 통계 프로그램 만들기

### 만들려고 한 것
반복문으로 점수 통계를 내는 프로그램

### 처음 막힌 부분
-1은 종료 신호이지만 동시에 0보다 작은 값이어서 조건을 어떤 순서로 검사해야 하는지 확인이 필요했다.(중요하다)

true의 대문자 표기와 학생 수를 출력하는 변수명을 잘못 씀.

### 내가 확인한 것
if score == -1:
    break
elif score < 0 or score > 100:
에서 -1도 0보다 작지만, 
먼저 종료 신호인지 검사하기 때문에
오류 점수로 처리되지 않고 정상적으로 종료된다.

### 해결한 방법
조건을 종료 신호, 잘못된 범위, 정상 점수 순서로 배치했다.

정상 점수만 입력하는 경우,
잘못된 점수가 포함된 경우,
점수 없이 바로 종료하는 경우를 각각 실행해 확인했다.

### 다음에 같은 문제를 만나면
특별한 값이 다른 조건에도 포함되는지 먼저 확인하고,
조건의 배치 순서를 테스트해본다.


## 2026-07-11 - 통계 변수의 초기화 위치를 수정한 경험

### 1. 상황

리스트를 이용한 점수 관리 프로그램을 만들면서
저장된 점수의 합계, 평균, 최댓값, 최솟값을 출력하는 기능을 구현하고 있었다.

### 2. 문제

처음에는 total을 scores처럼 while문 밖에 두어도 된다고 생각했다.
하지만 total을 프로그램 시작 때 한 번만 0으로 초기화하면,
통계 메뉴를 반복 실행할 때 이전 합계에 같은 점수가 다시 더해질 수 있었다.

### 3. 행동

scores와 total이 각각 언제까지 값을 유지해야 하는지 비교했다.
scores는 입력한 점수를 계속 저장해야 하므로 while문 밖에 두고,
total은 통계를 실행할 때마다 다시 계산하도록 3번 메뉴 안에서 0으로 초기화했다.
이후 같은 점수에서 통계 메뉴를 반복 실행해 결과를 확인했다.

### 4. 결과

점수 [80, 50, 100]에서 통계 메뉴를 실행했을 때
합계 230, 평균 76.66666666666667, 최댓값 100, 최솟값 50이 출력됐다.
통계를 다시 실행해도 합계가 중복되지 않고 같은 결과가 나왔다.

### 5. 배운 점

변수의 위치는 단순히 반복문 안과 밖으로 외우는 것이 아니라,
값을 얼마나 오래 유지해야 하며 언제 다시 초기화해야 하는지를 기준으로 정해야 한다.
앞으로 변수를 만들 때 프로그램 전체에서 유지할 값인지,
특정 기능을 실행할 때마다 초기화할 값인지 먼저 판단하겠다.


## 1주차 경험 후보: 점수 관리 프로그램 독립 재현

1. 어떤 과제를 수행했는가?
점수 추가, 전체 보기, 통계, 합격자 수, 마지막 점수 삭제 기능이 있는 점수 관리 프로그램 제작

2. 목표는 무엇이었는가?
점수 관리 프로그램의 독립 재현

3. 가장 크게 막힌 문제는 무엇이었는가?
코드 작성량 부족으로 인한 변수 초기화 위치, 코드 전체 구조 짜기

4. 처음에는 어떻게 작성했는가?
AI의 힌트를 보고 작성

5. 어떤 오류나 이상한 결과가 발생했는가?
리스트 변수가 정수로 바뀜, 변수 초기화 위치 실수

6. 내가 직접 확인한 것은 무엇인가?
변수의 위치를 변경하며 실행 결과 확인

7. 설명을 받아 알게 된 것은 무엇인가?
반복 중 유지할 변수와 매번 초기화할 변수를 구분해야 한다는 것

8. 무엇을 수정했는가?
누적·비교 변수 위치를 반복문 밖으로 수정

9. 수정 후 결과는 어떻게 달라졌는가?
통계 기능 정상 작동

10. 아직 혼자 해결하지 못하는 부분은 무엇인가?
전체 코드 골격 짜기

11. 같은 문제가 다시 나오면 무엇부터 확인할 것인가?
오류가 난 줄과 변수의 현재 자료형부터 확인

12. 자소서·면접 경험으로 사용할 가치가 있는가?
보조 경험 후보로는 사용 가능.
하지만 오류 원인을 혼자 찾은 경험은 아니므로
대표 문제 해결 경험으로 쓰기에는 아직 부족하다.

13. 사용한다면 어떤 역량을 보여주는가?
- 원본 없이 다시 구현하려고 시도한 태도
- 오류를 숨기지 않고 기록한 태도
- 수정 전후 실행 결과를 비교한 과정
- 부족한 부분을 다음 학습 목표로 연결한 점


## 2026-07-13 - 점수 관리 프로그램 함수 분리와 이름 충돌 해결

### 1. 상황
점수 관리 프로그램의 기능들을 밖으로 빼어내어 함수별 역할로 나누어 입력, 계산, 출력의 책임을 분리했다.

### 2. 문제
함수를 변수명으로 사용하여 오류가 발생했다.

### 3. 시도
변수명을 함수명과 다르게 고쳤다.


### 4. 결과
함수 이름과 변수 이름을 다르게 바꾼 뒤 오류가 사라졌고, 결과값이 정상적으로 출력됐다.


### 5. 나중에 쓸 수 있는 지점
긴 코드를 입력, 계산, 출력 기능으로 나누어 정리하고, 함수명과 변수명이 겹쳐 생긴 오류를 수정한 경험으로 쓸 수 있다.


## 2026-07-14 - 함수 연결 프로그램의 역할 중복 수정

함수 연결 프로그램을 독립 재현하며 역할 분리가 되지 않는 문제를 발견했고, 각 함수가 전달받은 값만 사용하도록 수정함.


## 2026-07-15 경험 후보

- 주제: 두 딕셔너리의 데이터를 같은 key로 연결한 경험
- 문제: 반복문 안에서 "노트"를 직접 적어 모든 상품에 같은 값이 적용됨
- 원인: items()가 반복마다 현재 상품과 가격을 전달한다는 점을 코드에 반영하지 못함
- 수정: purchase_quantities[product_name]으로 현재 상품의 수량을 조회함
- 결과: 상품별 금액과 전체 결제 금액 6500원을 정상 계산함


## 2026-07-16 - 순차 탐색 독립 재현에서 변수 역할을 혼동함

### 상황
리스트 순차 탐색 코드를 보지 않고 다시 작성했다.

### 문제
사용자가 입력한 이름을 저장한 target_name을 for문의 반복 변수로 다시 사용했다.
그 결과 입력값이 덮어써지고 첫 번째 요소에서 조건이 참이 되는 코드가 만들어졌다.

### 원인
찾을 값과 현재 비교 중인 값을 서로 다른 변수로 나누지 않았다.

### 수정
찾을 값은 target_name에 유지하고,
리스트에서 하나씩 꺼낸 값은 name에 저장해 target_name == name으로 비교했다.

### 배운 점
변수 이름은 단순한 표시가 아니라 코드 안에서 맡은 역할을 구분하는 수단이다.
독립 재현을 하면서 복사할 때는 보이지 않았던 변수 역할 혼동을 확인했다.


## 2026-07-17 - 리스트 내 딕셔너리 저장한 프로그램 함수이용작성

### 상황
리스트 안에 딕셔너리를 저장한 도서 관리 프로그램을 작성했다.

### 문제
책 제목 문자열과 book 딕셔너리 전체를 비교했다.
find_book()의 return None을 반복문 안에 두어 첫 번째 책만 확인했다.
가격을 수정할 때 현재 책 딕셔너리인 book이 아니라 리스트 전체인 books를 수정하려 했다.

### 수정
비교 대상을 book["title"]로 바꿨다.
return None을 반복문 밖으로 옮겼다.
가격 수정 대상을 book["price"] 또는 find_book()이 반환한 found_book["price"]로 바꿨다.

### 확인 결과
존재하는 책과 존재하지 않는 책을 모두 시험했다.
도서 관리 프로그램을 별도 파일에서 독립 재현했다.

### 배운 점
자료구조 전체와 그 안의 요소를 구분해야 한다.
return의 들여쓰기 위치가 탐색 범위를 결정할 수 있다.


## 2026-07-18 - 도서 관리 기능 독립 재현

### 해야 했던 일
도서 추가, 가격 수정, 삭제 기능을 하는 함수에 도서를 조회하는 함수호출을 넣어서 작성한 후 새 파일에 안 보고 재현해보았다.

### 처음 막힌 부분
각 기능에서 리스트를 다시 for문으로 탐색해야 하는지, 함수호출로 반환된 dictionary를 사용해야 하는지 구분하는 것이 어려웠다.

### 내가 확인한 방법
함수호출이 반환한 값을 추가·수정·삭제 함수에서 사용했다.
중복된 도서와 없는 도서도 실행해 True와 False가 예상대로 나오는지 확인했다.

### 결과
조회·추가·수정·삭제 함수를 새 파일에서 다시 작성했고, 최종 list가 예상한 dictionary의 항목만으로 남아있는 것을 확인했다.

### 아직 부족한 점
현재는 제목을 찾을 때마다 처음부터 순차 탐색한다. 데이터가 많아질 때 더 효율적으로 찾는 방법은 아직 모른다.


## 2026-07-19 - 도서 관리 기능 독립 재현

### 상황

2주차에 배운 함수와 리스트 안 딕셔너리를 사용해 도서 관리 핵심 기능을 자료 없이 다시 작성했다.

### 막힌 점

함수가 반환한 딕셔너리를 수정했을 때 원본까지 변경되는 이유를 정확히 설명하기 어려웠다.

### 확인한 과정

반환된 값과 books 안의 딕셔너리를 is와 id()로 비교했다.
원본을 직접 수정한 경우와 copy()를 사용한 경우를 각각 실행해 결과를 비교했다.

### 결과

find_book()이 원본 딕셔너리를 그대로 반환한다는 것을 확인했다.
도서 조회, 추가, 가격 수정, 삭제 함수를 독립적으로 다시 작성하고 실행했다.
원인을 파악한 뒤 가격 수정 함수는 원본을 그대로 쓰고 복사가 필요한 곳만 copy()를 쓰도록 구분했고, 자료 없이 CRUD 5개 기능을 다시 작성해 전부 정상 동작시켰다

### 배운 점

변수의 사용 범위와 객체의 생존 범위는 다르다.
함수의 반환값이 원본 객체인지 새 객체인지 구분해야 한다.
모르는 걸 추측으로 넘기지 않고 is와 id()로 직접 검증해서 확인하는 방식을 익혔다


## 2026-07-20 경험 후보

### 주제

인덱스 접근과 순차 탐색을 혼동한 문제

### 처음 작성한 방식

인덱스로 도서를 가져오는 함수에서도 for문으로 리스트를 순회했다.

```python
def get_book_by_index(books, index):
    for book in books:
        if book == books[index]:
            return book

    return None
```

### 문제

인덱스 접근을 코드에 쓴 것과 인덱스 접근으로 동작하도록 설계한 것은 다르다.
books[index]는 인덱스로 바로 접근하지만, 함수 전체에서는 for문으로 리스트를 처음부터 탐색하고 있었다.
따라서 함수 전체의 시간복잡도는 O(1)이 아니라 O(n)이었다.

### 수정

반복문을 제거하고 해당 인덱스의 값을 바로 반환했다.

```python
def get_book_by_index(books, index):
    return books[index]
```

### 확인한 내용

코드 일부에 O(1)인 연산이 들어 있어도 함수 전체가 O(1)이 되는 것은 아니다.
시간복잡도를 판단할 때는 함수 안에서 반복되는 전체 작업을 확인해야 한다.


## 2026-07-21 경험 후보: 파일 메모 프로그램 독립 재현

- 목표: 기존 코드를 보지 않고 파일 저장·조회·검색 프로그램 다시 만들기
- 문제: 공백 입력, 빈 검색어, 줄바꿈 출력 조건을 빠뜨림
- 수정: strip(), return, 출력용 문자열 분리 적용
- 확인 결과: 종료 후 재실행해도 데이터가 유지되고 검색 기능도 정상 작동함


## 2026-07-22 경험 후보

### 경험 후보
파일 기반 수정·삭제 프로그램을 독립 재현했다. 처음에는 수정 함수가 잘못됐다고 생각했지만, 실제 원인은 텍스트 파일의 각 제목 앞에 붙인 `#`였다. 일반 텍스트에서는 `#`도 데이터라는 것을 확인했고, 파일에서 읽은 값을 직접 출력해 원인을 찾았다. 삭제 함수에서는 리스트 변수 이름이 서로 다른 문제도 발견해 한 이름으로 통일했다.