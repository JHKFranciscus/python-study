# 경험 기록

## 2026-07-07

### 시작할 때 상태
어제 코딩 공부를 시작했고, 이틀 전에 Git을 설치했다. git add, git commit, git push라는 명령어 정도만 알고 있었다.

### 오늘 한 행동
master 브랜치를 main으로 변경한 일
git add와 git commit의 차이를 처음 구분한 일
mkdir를 mkidr로 잘못 입력하고 오류를 확인한 일
"문자열+정수+문자열"를 입력하고 오류를 확인한 일

### 막힌 문제
명령어의 개념을 명확히 하는 부분이 헷갈린다.
"문자열+정수+문자열"를 입력하고 오류가 난 것에서 해메었다.

### 해결과정
개념을 다시 종이에 필기해보았다.
문자열과 정수를 바로 더할 수 없다는 것을 확인해서, 정수를 str()로 바꾸는 법, 쉼표로 구분하는 법을 배웠고 f-string이라는 법도 있다는 것을 알게 되었다.
### 알게된 것
input으로 받은 값은 문자열이고, 계산하려면 int()로 바꿔야한다.
문자열과 정수는 +연산자로 바로 연결이 안 되므로 총 3가지의 방법으로 해결하는 것을 배웠다.
git add는 다음 commit에 포함할 변경 내용을 준비하고, git commit은 그 내용을 기록으로 남긴다.

### 다음 행동
내일 종이에 필기한 것을 다시 확인해본다.


## 2026-07-08 조건식 오류 수정

### 상황
시험 점수와 출석률을 각각 정수로 입력받고
int(), input(), if ,elif ,else ,and ,or을 사용하여
점수 또는 출석률이 0 미만이거나 100 초과이면
잘못된 입력입니다.
점수가 70 이상이고 출석률이 80 이상이면
수료입니다.
그 외에는
미수료입니다.
위 조건을 만족하는 수료 판정 프로그램을 작성했다.

### 문제
점수 105, 출석률 90을 입력했을 때
잘못된 입력이 아니라 수료라고 출력됐다.

### 내가 처음 시도한 방법
두 변수의 범위를 한꺼번에 검사하려고 (score or check_rate) < 0 and (score or check_rate) > 100을 사용했다.

### 잘못된 이유
`score or check_rate`는 두 변수를 모두 검사하는 표현이 아니었다.
두 값 중 참으로 취급되는 한 값을 반환하므로, 각 변수의 범위를 따로 비교하지 못했다.

또한 하나의 값이 동시에 0보다 작고 100보다 클 수 없으므로
`값 < 0 and 값 > 100` 조건은 항상 거짓이었다.

### 수정한 방법
score와 check_rate를 각각 비교하도록 조건식을 score < 0 or score > 100 or check_rate < 0 or check_rate > 100으로 수정함.
점수 85·출석률 105, 점수 85·출석률 -1을 입력해 다시 확인함.

### 결과
점수 85·출석률 105와 점수 85·출석률 -1을 입력했을 때
모두 `잘못된 입력입니다.`가 출력되는 것을 확인했다.
정상 범위의 입력에서는 수료와 미수료도 기준에 맞게 출력됐다.

### 이 경험에서 배운 점
여러 변수의 범위를 검사할 때는 변수마다 비교식을 따로 작성해야 한다.
코드가 실행된다는 것만으로 맞다고 판단하지 말고,
105, -1 같은 잘못된 입력과 경계값을 직접 넣어 확인해야 한다.


## 2026-07-09 Day 4 - 조건문을 배우면서 헷갈린 개념을 정리한 경험

### 상황
Python 조건문을 배우면서 if / elif / else, input, int, and 조건을 연습했다.  
처음에는 코드를 바꿔서 테스트해야 하는데, 기존 코드 아래에 계속 추가해서 실행 결과가 여러 번 나오는 실수를 했다.

### 문제
조건문 자체보다도 코드를 어떻게 테스트해야 하는지, 그리고 =와 ==의 차이를 구분하는 것이 헷갈렸다.  
또 input으로 받은 값이 바로 숫자가 아니라 문자열이라는 점도 처음에는 익숙하지 않았다.
if ticket == yes (x) if ticket == "yes" (o)

### 내가 한 행동
코드를 한 번에 계속 추가하지 않고, 기존 값을 바꿔가며 다시 실행하는 방식으로 고쳤다.  
점수 등급, 나이 판별, 짝수/홀수 판별, 티켓 입장 조건 문제를 직접 실행해 보면서 조건문 구조를 반복했다.  
또 공부일지에 =는 값 저장, ==는 비교, signed integer와 unsigned integer의 차이를 따로 정리했다.
비교 연산자에서 정의된 변수가 아닌 문자열에는 " "를 붙여주었다.

### 배운 점
조건문은 단순히 문법을 외우는 것이 아니라, 문제의 조건을 코드의 비교식으로 바꾸는 연습이 필요하다는 것을 알게 되었다.  
또 input은 문자열로 들어오기 때문에 숫자 비교를 하려면 int로 바꿔야 한다는 점을 알게 되었다.

### 자소서나 면접에 쓸 수 있는 포인트
기초 개념을 배울 때 실수를 그냥 넘기지 않고, 왜 틀렸는지 기록하고 다시 실행하면서 수정했다.  
처음에는 따라 치는 수준이었지만, 점점 문제 조건을 코드로 바꾸는 방식에 익숙해지고 있다.


## 2026-07-10 Day 5 - 반복문으로 점수 통계 프로그램 만들기

### 만들려고 한 것
반복문으로 점수 통계를 내는 프로그램

### 처음 막힌 부분
-1은 종료 신호이지만 동시에 0보다 작은 값이어서 조건을 어떤 순서로 검사해야 하는지 확인이 필요했다.(중요하다)

true의 대문자 표기와 학생 수를 출력하는 변수명을 잘못 씀.

### 내가 확인한 것
if score == -1:
    break
elif score < 0 or score > 100:
에서 -1도 0보다 작지만, 
먼저 종료 신호인지 검사하기 때문에
오류 점수로 처리되지 않고 정상적으로 종료된다.

### 해결한 방법
조건을 종료 신호, 잘못된 범위, 정상 점수 순서로 배치했다.

정상 점수만 입력하는 경우,
잘못된 점수가 포함된 경우,
점수 없이 바로 종료하는 경우를 각각 실행해 확인했다.

### 다음에 같은 문제를 만나면
특별한 값이 다른 조건에도 포함되는지 먼저 확인하고,
조건의 배치 순서를 테스트해본다.


## 2026-07-11 - 통계 변수의 초기화 위치를 수정한 경험

### 1. 상황

리스트를 이용한 점수 관리 프로그램을 만들면서
저장된 점수의 합계, 평균, 최댓값, 최솟값을 출력하는 기능을 구현하고 있었다.

### 2. 문제

처음에는 total을 scores처럼 while문 밖에 두어도 된다고 생각했다.
하지만 total을 프로그램 시작 때 한 번만 0으로 초기화하면,
통계 메뉴를 반복 실행할 때 이전 합계에 같은 점수가 다시 더해질 수 있었다.

### 3. 행동

scores와 total이 각각 언제까지 값을 유지해야 하는지 비교했다.
scores는 입력한 점수를 계속 저장해야 하므로 while문 밖에 두고,
total은 통계를 실행할 때마다 다시 계산하도록 3번 메뉴 안에서 0으로 초기화했다.
이후 같은 점수에서 통계 메뉴를 반복 실행해 결과를 확인했다.

### 4. 결과

점수 [80, 50, 100]에서 통계 메뉴를 실행했을 때
합계 230, 평균 76.66666666666667, 최댓값 100, 최솟값 50이 출력됐다.
통계를 다시 실행해도 합계가 중복되지 않고 같은 결과가 나왔다.

### 5. 배운 점

변수의 위치는 단순히 반복문 안과 밖으로 외우는 것이 아니라,
값을 얼마나 오래 유지해야 하며 언제 다시 초기화해야 하는지를 기준으로 정해야 한다.
앞으로 변수를 만들 때 프로그램 전체에서 유지할 값인지,
특정 기능을 실행할 때마다 초기화할 값인지 먼저 판단하겠다.


## 1주차 경험 후보: 점수 관리 프로그램 독립 재현

1. 어떤 과제를 수행했는가?
점수 추가, 전체 보기, 통계, 합격자 수, 마지막 점수 삭제 기능이 있는 점수 관리 프로그램 제작

2. 목표는 무엇이었는가?
점수 관리 프로그램의 독립 재현

3. 가장 크게 막힌 문제는 무엇이었는가?
코드 작성량 부족으로 인한 변수 초기화 위치, 코드 전체 구조 짜기

4. 처음에는 어떻게 작성했는가?
AI의 힌트를 보고 작성

5. 어떤 오류나 이상한 결과가 발생했는가?
리스트 변수가 정수로 바뀜, 변수 초기화 위치 실수

6. 내가 직접 확인한 것은 무엇인가?
변수의 위치를 변경하며 실행 결과 확인

7. 설명을 받아 알게 된 것은 무엇인가?
반복 중 유지할 변수와 매번 초기화할 변수를 구분해야 한다는 것

8. 무엇을 수정했는가?
누적·비교 변수 위치를 반복문 밖으로 수정

9. 수정 후 결과는 어떻게 달라졌는가?
통계 기능 정상 작동

10. 아직 혼자 해결하지 못하는 부분은 무엇인가?
전체 코드 골격 짜기

11. 같은 문제가 다시 나오면 무엇부터 확인할 것인가?
오류가 난 줄과 변수의 현재 자료형부터 확인

12. 자소서·면접 경험으로 사용할 가치가 있는가?
보조 경험 후보로는 사용 가능.
하지만 오류 원인을 혼자 찾은 경험은 아니므로
대표 문제 해결 경험으로 쓰기에는 아직 부족하다.

13. 사용한다면 어떤 역량을 보여주는가?
- 원본 없이 다시 구현하려고 시도한 태도
- 오류를 숨기지 않고 기록한 태도
- 수정 전후 실행 결과를 비교한 과정
- 부족한 부분을 다음 학습 목표로 연결한 점


## 2026-07-13 - 점수 관리 프로그램 함수 분리와 이름 충돌 해결

### 1. 상황
점수 관리 프로그램의 기능들을 밖으로 빼어내어 함수별 역할로 나누어 입력, 계산, 출력의 책임을 분리했다.

### 2. 문제
함수를 변수명으로 사용하여 오류가 발생했다.

### 3. 시도
변수명을 함수명과 다르게 고쳤다.


### 4. 결과
함수 이름과 변수 이름을 다르게 바꾼 뒤 오류가 사라졌고, 결과값이 정상적으로 출력됐다.


### 5. 나중에 쓸 수 있는 지점
긴 코드를 입력, 계산, 출력 기능으로 나누어 정리하고, 함수명과 변수명이 겹쳐 생긴 오류를 수정한 경험으로 쓸 수 있다.


## 2026-07-14 - 함수 연결 프로그램의 역할 중복 수정

함수 연결 프로그램을 독립 재현하며 역할 분리가 되지 않는 문제를 발견했고, 각 함수가 전달받은 값만 사용하도록 수정함.


## 2026-07-15 경험 후보

- 주제: 두 딕셔너리의 데이터를 같은 key로 연결한 경험
- 문제: 반복문 안에서 "노트"를 직접 적어 모든 상품에 같은 값이 적용됨
- 원인: items()가 반복마다 현재 상품과 가격을 전달한다는 점을 코드에 반영하지 못함
- 수정: purchase_quantities[product_name]으로 현재 상품의 수량을 조회함
- 결과: 상품별 금액과 전체 결제 금액 6500원을 정상 계산함


## 2026-07-16 - 순차 탐색 독립 재현에서 변수 역할을 혼동함

### 상황
리스트 순차 탐색 코드를 보지 않고 다시 작성했다.

### 문제
사용자가 입력한 이름을 저장한 target_name을 for문의 반복 변수로 다시 사용했다.
그 결과 입력값이 덮어써지고 첫 번째 요소에서 조건이 참이 되는 코드가 만들어졌다.

### 원인
찾을 값과 현재 비교 중인 값을 서로 다른 변수로 나누지 않았다.

### 수정
찾을 값은 target_name에 유지하고,
리스트에서 하나씩 꺼낸 값은 name에 저장해 target_name == name으로 비교했다.

### 배운 점
변수 이름은 단순한 표시가 아니라 코드 안에서 맡은 역할을 구분하는 수단이다.
독립 재현을 하면서 복사할 때는 보이지 않았던 변수 역할 혼동을 확인했다.


## 2026-07-17 - 리스트 내 딕셔너리 저장한 프로그램 함수이용작성

### 상황
리스트 안에 딕셔너리를 저장한 도서 관리 프로그램을 작성했다.

### 문제
책 제목 문자열과 book 딕셔너리 전체를 비교했다.
find_book()의 return None을 반복문 안에 두어 첫 번째 책만 확인했다.
가격을 수정할 때 현재 책 딕셔너리인 book이 아니라 리스트 전체인 books를 수정하려 했다.

### 수정
비교 대상을 book["title"]로 바꿨다.
return None을 반복문 밖으로 옮겼다.
가격 수정 대상을 book["price"] 또는 find_book()이 반환한 found_book["price"]로 바꿨다.

### 확인 결과
존재하는 책과 존재하지 않는 책을 모두 시험했다.
도서 관리 프로그램을 별도 파일에서 독립 재현했다.

### 배운 점
자료구조 전체와 그 안의 요소를 구분해야 한다.
return의 들여쓰기 위치가 탐색 범위를 결정할 수 있다.


## 2026-07-18 - 도서 관리 기능 독립 재현

### 해야 했던 일
도서 추가, 가격 수정, 삭제 기능을 하는 함수에 도서를 조회하는 함수호출을 넣어서 작성한 후 새 파일에 안 보고 재현해보았다.

### 처음 막힌 부분
각 기능에서 리스트를 다시 for문으로 탐색해야 하는지, 함수호출로 반환된 dictionary를 사용해야 하는지 구분하는 것이 어려웠다.

### 내가 확인한 방법
함수호출이 반환한 값을 추가·수정·삭제 함수에서 사용했다.
중복된 도서와 없는 도서도 실행해 True와 False가 예상대로 나오는지 확인했다.

### 결과
조회·추가·수정·삭제 함수를 새 파일에서 다시 작성했고, 최종 list가 예상한 dictionary의 항목만으로 남아있는 것을 확인했다.

### 아직 부족한 점
현재는 제목을 찾을 때마다 처음부터 순차 탐색한다. 데이터가 많아질 때 더 효율적으로 찾는 방법은 아직 모른다.


## 2026-07-19 - 도서 관리 기능 독립 재현

### 상황

2주차에 배운 함수와 리스트 안 딕셔너리를 사용해 도서 관리 핵심 기능을 자료 없이 다시 작성했다.

### 막힌 점

함수가 반환한 딕셔너리를 수정했을 때 원본까지 변경되는 이유를 정확히 설명하기 어려웠다.

### 확인한 과정

반환된 값과 books 안의 딕셔너리를 is와 id()로 비교했다.
원본을 직접 수정한 경우와 copy()를 사용한 경우를 각각 실행해 결과를 비교했다.

### 결과

find_book()이 원본 딕셔너리를 그대로 반환한다는 것을 확인했다.
도서 조회, 추가, 가격 수정, 삭제 함수를 독립적으로 다시 작성하고 실행했다.
원인을 파악한 뒤 가격 수정 함수는 원본을 그대로 쓰고 복사가 필요한 곳만 copy()를 쓰도록 구분했고, 자료 없이 CRUD 5개 기능을 다시 작성해 전부 정상 동작시켰다

### 배운 점

변수의 사용 범위와 객체의 생존 범위는 다르다.
함수의 반환값이 원본 객체인지 새 객체인지 구분해야 한다.
모르는 걸 추측으로 넘기지 않고 is와 id()로 직접 검증해서 확인하는 방식을 익혔다


## 2026-07-20 경험 후보

### 주제

인덱스 접근과 순차 탐색을 혼동한 문제

### 처음 작성한 방식

인덱스로 도서를 가져오는 함수에서도 for문으로 리스트를 순회했다.

```python
def get_book_by_index(books, index):
    for book in books:
        if book == books[index]:
            return book

    return None
```

### 문제

인덱스 접근을 코드에 쓴 것과 인덱스 접근으로 동작하도록 설계한 것은 다르다.
books[index]는 인덱스로 바로 접근하지만, 함수 전체에서는 for문으로 리스트를 처음부터 탐색하고 있었다.
따라서 함수 전체의 시간복잡도는 O(1)이 아니라 O(n)이었다.

### 수정

반복문을 제거하고 해당 인덱스의 값을 바로 반환했다.

```python
def get_book_by_index(books, index):
    return books[index]
```

### 확인한 내용

코드 일부에 O(1)인 연산이 들어 있어도 함수 전체가 O(1)이 되는 것은 아니다.
시간복잡도를 판단할 때는 함수 안에서 반복되는 전체 작업을 확인해야 한다.


## 2026-07-21 경험 후보: 파일 메모 프로그램 독립 재현

- 목표: 기존 코드를 보지 않고 파일 저장·조회·검색 프로그램 다시 만들기
- 문제: 공백 입력, 빈 검색어, 줄바꿈 출력 조건을 빠뜨림
- 수정: strip(), return, 출력용 문자열 분리 적용
- 확인 결과: 종료 후 재실행해도 데이터가 유지되고 검색 기능도 정상 작동함


## 2026-07-22 경험 후보

### 경험 후보
파일 기반 수정·삭제 프로그램을 독립 재현했다. 처음에는 수정 함수가 잘못됐다고 생각했지만, 실제 원인은 텍스트 파일의 각 제목 앞에 붙인 `#`였다. 일반 텍스트에서는 `#`도 데이터라는 것을 확인했고, 파일에서 읽은 값을 직접 출력해 원인을 찾았다. 삭제 함수에서는 리스트 변수 이름이 서로 다른 문제도 발견해 한 이름으로 통일했다.

## 2026-07-23 경험 후보: JSON 도서 관리 프로그램 독립 재현

### 상황

앞에서 만든 JSON 기반 도서 관리 프로그램을 원본 코드를 보지 않고 별도 파일에서 다시 구현했다.

### 처음 예상

같은 기능을 다시 만드는 것이니 원본과 비슷한 수준으로 나올 것이라고 예상
기능은 비슷하게 만들었지만 원본에서 신경 쓰지 않았던 부분에서 문제가 발생

### 발생한 문제

가격을 바로 int로 변환해서 숫자가 아닌 값을 입력해서 오류가 발생
빈 검색어와 없는 검색어를 사용했을 때 모두 빈 리스트로 반환하여 두 상황을 구분하기 어려웠다.

### 확인한 과정

가격을 int로 바꾸기 전에 isdigit()으로 검사했다.
빈 검색어에는 None을 반환하고, 정상 검색 후 결과가 없는 경우에는 빈 리스트를 반환하도록 나눴다.
abc 가격 입력과 빈 검색어 입력을 직접 시험했다.
프로그램을 종료하고 다시 실행한 뒤 수정·삭제 결과가 JSON 파일에 남아 있는지도 확인했다.

### 수정한 방법

숫자가 아닌 가격은 안내 문구를 출력하고 함수 실행을 끝내도록 수정
None과 빈 리스트에 서로 다른 의미를 부여하고 메뉴에서 각각 따로 처리

### 결과

잘못된 가격을 입력해도 프로그램이 종료되지 않고 메뉴로 돌아갔다.
빈 검색어 안내 문구가 한 번만 출력됐다.
도서 추가, 조회, 검색, 가격 수정, 삭제와 재실행 후 데이터 유지까지 작동했다.

### 경험 후보로 남기는 이유

완성된 코드를 따라 친 것이 아니라 보지 않고 다시 만들었고, 그 과정에서 원본에 있던 입력값과 반환값의 의미 중복을 발견했다. isdigit() 검사를 이용하여 수정한 경험이다.


## 2026-07-24 모듈형 메모 관리 프로그램 독립 재현

### 상황
JSON 도서 관리 프로그램을 모듈로 분리한 뒤,
기존 코드를 보지 않고 메모 관리 프로그램을 다시 만들었다.

### 예상
파일별 역할을 이해했으므로 같은 구조를 바로 재현할 수 있을 것으로 예상했다.

### 문제
main, service, storage의 큰 역할은 나눴지만
함수의 인자와 반환값, 파일 이름, 딕셔너리 값 접근을 여러 번 혼동했다.

### 시도
traceback의 파일명과 줄 번호를 따라가며 오류를 하나씩 수정했다.
함수 반환값을 먼저 받은 뒤 저장과 출력을 결정하도록 흐름을 다시 정리했다.

### 결과
메모 추가·조회·검색·JSON 저장과 재실행을 정상 동작시켰다.

### 배운 점
모듈 분리는 파일만 나누는 것이 아니라
각 함수의 입력값·반환값·다음 호출 흐름까지 정하는 작업이다.


## 2026-07-25 예외 처리가 포함된 모듈형 프로그램 독립 재현

- 상황: 기존 코드를 보지 않고 main·service·storage로 나눈 도서 관리 프로그램을 다시 작성하면서 잘못된 입력과 파일 오류도 처리해야 했다.
- 예상: 파일이 없거나 JSON이 손상되면 프로그램이 종료될 것으로 예상했고, return과 예외 전파의 차이도 바로 구분하지 못했다.
- 행동: abc·음수·정상 가격을 각각 시험하고, JSON을 일부러 손상시켜 FileNotFoundError·JSONDecodeError 처리와 저장 후 재실행을 확인했다.
- 결과: 잘못된 가격은 저장되지 않았고 정상 도서는 재실행 후에도 유지됐다. 손상된 JSON에서도 traceback 없이 빈 목록으로 프로그램이 실행됐다.
- 배운 점: 예외 상황에서도 함수가 기대하는 자료형을 유지해 반환해야 이후 코드가 계속 작동하며, main·service·storage의 역할을 분리하면 코드 중복을 줄일 수 있다.

## 2026-07-26 경험 후보: 모듈형 도서 관리 프로그램 구조 수정

## 상황

JSON 기반 도서 관리 프로그램을 `main`, `service`, `storage`로 나누어 작성했다.

## 문제

검색 함수가 `True` 또는 `False`만 반환해 `main`에서 같은 검색을 다시 수행했다.

또한 파일 예외 처리와 가격 변환 위치가 섞여 각 모듈의 역할이 명확하지 않았다.

## 행동

- 검색 함수가 검색된 도서 목록을 반환하도록 수정했다.
- 파일 읽기·저장은 `storage`, 데이터 처리는 `service`가 담당하게 했다.
- 사용자 입력·출력, 정수 변환, 최종 예외 처리는 `main`으로 옮겼다.
- 파일 없음, 잘못된 JSON, `abc` 가격 입력, 대소문자가 다른 제목을 직접 시험했다.

## 결과

- 검색 중복을 제거했다.
- 대소문자가 달라도 검색·수정·삭제가 가능해졌다.
- 잘못된 가격과 JSON 파일에도 프로그램이 종료되지 않았다.
- 프로그램을 재실행해도 수정된 데이터가 유지됐다.

## 배운 점

함수를 작성하기 전에 반환값과 모듈별 역할을 정해야 중복 코드와 역할 혼합을 줄일 수 있다는 것을 배웠다.


## 2026-07-27 경험후보: 클래스 기반 도서 관리 프로그램 독립 재현

### 상황

클래스와 인스턴스 메서드를 학습한 뒤, 완성 코드를 보지 않고 도서 관리 프로그램을 다시 구현했다.

### 예상

`Book` 객체를 리스트에 저장하고 등록·조회·검색·가격 변경 기능을 함수로 나누면 구현할 수 있을 것으로 생각했다.

### 문제

`self`로 접근할 수 있는 값을 메서드 인수로 다시 전달했고, 반환값이 없는 조회 함수를 실패로 판단해 책이 있는데도 `"책이 없습니다."`가 출력됐다. 문자로 가격을 입력했을 때는 `ValueError`도 발생했다.

### 행동

메서드가 `self`를 통해 속성에 접근하도록 수정하고, 조회 함수가 빈 목록일 때와 책이 있을 때 반환할 값을 정했다. 가격 입력에는 `int()`와 `try-except`를 적용했다.

### 결과

책 세 권의 등록·조회·검색·가격 변경을 정상적으로 실행했고, 잘못된 가격을 입력해도 프로그램이 종료되지 않도록 수정했다.

### 배운 점

함수의 역할뿐 아니라 반환값의 기준도 먼저 정해야 호출하는 쪽의 조건문을 정확하게 작성할 수 있다는 점을 확인했다.


## 2026 07 28 경험일지 - 클래스 기반 도서 관리 프로그램 독립 재현

### 상황

클래스 속성과 인스턴스 메서드, 클래스 메서드, 정적 메서드를 사용한 도서 관리 프로그램을 기존 코드를 보지 않고 다시 작성했다.

### 문제

가격 검사 함수를 None과 False와 정수를 함께 반환하도록 작성했다. 그런데 파이썬에서 0 == False가 참이라, 가격이 0일 때 정상 입력이 실패로 처리됐다. 또 검사 과정에서 정수로 변환은 했지만, 실제 객체에는 변환 전 입력 문자열을 저장했다. 존재하지 않는 도서를 검색했을 때 반환되는 None도 처리하지 않았다.

### 시도

가격 검사 함수는 True와 False만 반환하도록 수정했다. 성공했는지 아닌지만 판단하면 되는 함수라 반환값을 두 가지로 통일했다. 입력값의 정수 변환은 입력을 받는 함수에서 처리하고, change_price()는 가격 검사와 변경 성공 여부 반환을 담당하도록 역할을 나눴다. 그 뒤 가격 0 등록, 존재하지 않는 도서의 가격 변경, 음수 가격 변경을 다시 테스트했다.

### 결과

가격 0이 정상적으로 등록됐고, 정상 가격은 정수로 저장됐다. 존재하지 않는 도서를 입력해도 프로그램이 종료되지 않았다. 음수 가격으로 변경에 실패했을 때는 기존 가격이 그대로 유지됐다.

### 배운 점

반환값의 종류를 섞으면 호출하는 쪽의 판단이 복잡해진다. 특히 값과 False를 함께 반환하면 0을 실패와 구분할 수 없다. 입력 변환과 유효성 검사, 객체 상태 변경은 역할을 나눠야 한다. 경계값과 실패 상황은 따로 테스트해야 한다.


## 2026 07 29 경험일지 - 상속 기반 상품 관리 프로그램 독립 재현

### 상황
상속과 객체 구성 관계를 학습한 뒤, 기존 도서 관리 코드를 보지 않고 `Product`, `DownloadProduct`, `ProductManager` 구조를 다시 구현했다.

### 처음 예상
`find_product()`가 상품을 찾지 못하는 것은 곧 실패를 뜻한다고 생각했다. 그래서 이후 조건문에서 `False`와 비교해도 된다고 봤다.

### 발생한 문제
`find_product()`는 검색에 실패하면 `None`을 반환하도록 작성했다. 그런데 `change_product_price()`에서는 이렇게 검사했다.

```python
if change_product is not False:
```

`None is not False`의 결과는 `True`다. 그래서 존재하지 않는 상품의 가격을 변경하면 `None.change_price()`를 호출하게 되는 구조였다.

전체 조회 결과를 담는 리스트를 어디에서 만들어야 하는지, 가격 변경 메서드를 어떤 객체에서 호출해야 하는지에서도 막혔다.

### 확인한 과정
- `find_product()`의 실제 실패 반환값이 `None`인지 확인했다.
- `None is not False`의 결과를 코드 흐름과 대조했다.
- 존재하지 않는 상품의 가격을 변경하는 테스트를 추가했다.
- 관리 클래스가 직접 가격을 바꾸는 것이 아니라, 검색한 상품 객체의 `change_price()`를 호출해야 한다는 것을 다시 확인했다.

### 수정한 방법
```python
def change_product_price(self, target_name, new_price):
    change_product = self.find_product(target_name)
    if change_product is not None:
        return change_product.change_price(new_price)
    return False
```

검색 실패 여부는 `None`으로 확인하고, 가격 변경 작업의 성공 여부는 `True`와 `False`로 반환하도록 역할을 나눴다.

### 실제 결과
존재하지 않는 상품의 가격 변경을 시도했을 때 오류가 발생하지 않고 `False`가 반환됐다. 기존 상품의 가격도 바뀌지 않았다.

### 배운 점
같은 '실패'처럼 보여도 메서드의 역할에 따라 반환값의 의미가 다르다. 객체 검색 실패에는 `None`, 변경이나 삭제 같은 작업 실패에는 `False`를 사용한다. 그리고 호출하는 쪽에서는 그 메서드의 반환 규칙에 맞춰 조건을 작성해야 한다.


## 2026-07-30 경험 후보 — JSON 상품 관리 프로그램

### 한 줄 요약

실행할 때마다 JSON 파일을 삭제해 저장값이 사라졌고, 삭제 코드를 제거한 뒤에는 초기 상품이 중복 등록될 수 있었다. 테스트용 초기화와 실제 데이터 유지 코드를 구분하고 저장·복원 흐름을 단계별로 확인하여 두 문제를 해결했다.

### 경험 제목

JSON 기반 상품 관리 프로그램에서 데이터 유지 문제와 중복 등록 문제를 구분해 수정한 경험

### 상황

Python의 클래스와 상속, JSON 저장·불러오기를 학습한 뒤, 도서 관리 코드를 보지 않고 `Product`, `DownloadProduct`, `ProductManager`를 사용하는 상품 관리 프로그램을 처음부터 다시 구현했다.

프로그램에는 상품 등록, 전체 조회, 검색, 가격 변경, 삭제, JSON 저장과 복원 기능이 필요했다. 저장한 데이터를 다시 불러왔을 때 일반 상품과 다운로드 상품의 클래스 종류까지 복원되어야 했다.

### 문제

처음에는 테스트를 항상 같은 상태에서 시작하려고 프로그램 실행 시 기존 JSON 파일을 삭제했다. 이 방식에서는 실행할 때마다 데이터가 초기화되어 이전 실행에서 저장한 값이 유지되지 않았다.

파일 삭제 코드를 제거한 뒤에는 기존 상품을 불러온 상태에서도 초기 상품을 다시 등록해서, 같은 상품이 중복으로 추가될 가능성이 생겼다.

또한 `json.load()`가 JSON 데이터를 `list`와 `dict`로 바꾸는 단계와, 그 딕셔너리로 `Product` 계열 인스턴스를 만드는 단계를 처음에는 정확히 구분하지 못했다.

### 원인

- 메모리의 객체와 디스크의 JSON 파일을 별개의 데이터로 보지 못했다.
- 테스트용 초기화 코드와 실제 데이터를 유지하는 코드를 구분하지 못했다.
- 기존 데이터가 있는지 확인하지 않고 초기 상품을 무조건 등록했다.
- 역직렬화와 사용자 정의 클래스 객체 복원을 하나의 과정으로 생각했다.

### 시도와 수정

- 실행할 때마다 JSON 파일을 삭제하던 코드를 제거했다. [피드백]
- 불러온 상품 목록이 비어 있을 때만 초기 상품을 등록하도록 조건을 추가했다. [피드백]
- 객체를 `to_dict()`로 변환하면서 `type` 값을 함께 저장했다.
- `create_product_from_dict()`에서 `type`을 확인해 `Product` 또는 `DownloadProduct` 인스턴스를 새로 생성했다.
- 알 수 없는 `type`이라 복원 함수가 `None`을 반환하면 상품 목록에 추가하지 않았다. [피드백]
- 가격 변경 시 숫자로 된 문자열은 `int`로 변환하고, 숫자가 아닌 값과 음수는 거부했다.
- 가격 변환 과정에서 `ValueError`와 `TypeError`를 함께 처리했다. [피드백]
- 저장 후 새로운 `ProductManager`를 만들어 클래스 종류와 가격이 유지되는지 확인했다.

### 결과

- 초기 등록 단계에서 키보드와 게임이 각각 한 개씩만 등록되는 것을 확인했다.
- 게임 가격을 문자열 `"35000"`으로 입력했을 때 `int` 형 `35000`으로 바뀌는 것을 확인했다.
- 키보드를 삭제하고 저장한 뒤 JSON 파일에는 게임 한 개만 남았다.
- 새로운 `ProductManager`에서 게임이 `DownloadProduct` 인스턴스로 복원됐다.
- 복원된 가격이 `35000`으로 유지됐고, 복원 후에도 검색·가격 변경·삭제 기능을 사용할 수 있었다.

### 배운 점

메모리에서 객체를 수정하는 것과 파일의 데이터를 수정하는 것은 별개의 작업이다. 바뀐 값을 프로그램 종료 후에도 유지하려면 객체를 JSON이 처리할 수 있는 자료형으로 바꾼 뒤 파일을 다시 기록해야 한다.

`json.load()`는 JSON 데이터를 Python의 `list`와 `dict`로 역직렬화할 뿐이고, 사용자 정의 클래스 인스턴스는 별도의 복원 함수가 만들어야 한다.

데이터를 유지하는 프로그램에서는 초기 데이터를 등록하는 조건도 저장·복원 구조의 일부다. 기존 데이터가 있는지 확인하지 않으면 중복이 생길 수 있다.

### 직접 구현하고 확인한 부분

- `Product`와 `DownloadProduct`의 상속 및 메서드 오버라이딩
- 객체를 딕셔너리로 바꾸는 `to_dict()`
- `type`을 이용한 클래스 복원
- 상품 검색, 가격 변경, 삭제
- JSON 저장 후 새로운 관리 객체에서 복원 확인
- 가격의 값과 자료형 확인
- 삭제 전후 상품 수와 JSON 파일 내용 확인

피드백을 받아 고친 부분은 「시도와 수정」에 [피드백]으로 표시했다. 그 밖에 `json.load()`의 역직렬화와 클래스 객체 복원을 별도 단계로 정리한 것도 피드백을 받은 부분이다.

### 활용 가능성

- SSAFY 자기소개서의 문제 해결 경험 후보
- 면접에서 파일 저장과 메모리 데이터의 차이를 설명하는 사례
- 코드를 보지 않고 다시 구현하는 과정에서 오류를 발견하고 수정한 경험
- 테스트용 코드와 실제 프로그램 코드를 구분한 경험


## 2026-07-31 경험 후보 - JSON 상품 관리 프로그램에서 복원된 객체를 실제로 확인하도록 구조 수정

### 상황
캡슐화와 getter·setter를 적용한 상품 객체를 JSON에 저장하고 복원하는 상품 관리 프로그램을 요구사항만 보고 독립 재현했다. 프로그램은 일반 상품과 다운로드 상품을 저장한 뒤, 새로운 `ProductManager`에서 실제 클래스의 객체로 복원하고 검색·가격 변경·삭제까지 수행해야 했다.

### 발생한 문제
처음 실행했을 때 저장과 조회 결과는 출력됐지만, 코드를 다시 보니 복원 기능을 실제로 확인한 것이 아니었다. 먼저 `ProductManager`가 파일을 직접 읽지 않고 외부에서 목록을 전달받도록 작성했다. `load_products()`는 반환문 위치를 잘못 잡아 반복문이 끝나기 전에 함수가 종료될 수 있는 구조였다. 마지막으로 파일에서 새 목록을 복원한 뒤에도 새 관리자에 기존 메모리 목록을 넣어, 복원된 객체가 아니라 원래 있던 객체를 확인하고 있었다.

### 원인
각 함수가 맡아야 할 역할과 데이터가 이동하는 경로를 구분하지 않고, 최종 출력이 예상과 맞는지만 확인한 것이 원인이었다. 특히 기존 객체와 JSON에서 새로 복원된 객체를 구분하지 않아, 복원 기능이 실제로 작동했는지 판단할 수 없었다.

### 수정 및 확인 과정
- `ProductManager(filename)`이 생성될 때 `load_products(filename)`을 직접 호출하도록 수정했다.
- `load_products()`의 반환문을 반복문 밖으로 옮겨, 모든 상품을 처리한 뒤 반환하도록 했다.
- 알 수 없는 상품 종류에 대해 반환된 `None`은 목록에 넣지 않도록 했다.
- 테스트에서 기존 관리자가 아니라 새로 생성한 관리자를 사용했다.
- 복원된 각 객체의 클래스를 직접 출력해 `Product`와 `DownloadProduct`가 맞는지 확인했다.
- 복원된 다운로드 상품에 정상 가격, 문자열, 음수를 차례로 대입해 setter가 작동하는지 확인했다.

### 결과
JSON에 저장한 상품 두 개가 각각 `Product`와 `DownloadProduct` 객체로 복원됐다. 복원된 다운로드 상품의 가격을 `90000`으로 변경했을 때는 정상 반영됐고, 문자열과 음수는 거부된 뒤 기존 가격 `90000`이 유지됐다. 다운로드 상품을 검색하였을 때는 일반 상품이 아닌 다운로드 상품이 검색된느 것을 확인하였다.일반 상품을 삭제한 뒤 남은 상품이 1개인 것을 확인하고 다시 저장했다.

### 배운 점
출력이 예상과 같더라도, 실제로 어떤 데이터와 객체를 확인했는지 따지지 않으면 기능이 정상이라고 판단할 수 없다. 독립 재현에서는 기능을 구현하는 것뿐 아니라 각 클래스와 함수의 역할, 데이터가 이동하는 경로, 값을 반환하는 시점, 실제 테스트 대상까지 함께 확인해야 한다.

### 아직 부족한 점
지금은 JSON에 잘못된 가격이 들어 있으면 setter가 `ValueError`를 발생시키면서 프로그램 전체가 종료된다. 정상 상품은 복원하면서 잘못된 상품만 제외하는 처리는 아직 구현하지 않았다.


## 2026-08-01 경험 후보

### 경험 제목
파일 읽기와 객체 생성의 책임을 분리하며 일반 함수와 인스턴스 메서드의 차이를 확인한 경험

### 상황
상품 정보를 JSON으로 저장하고 다시 각 상품 클래스의 객체로 복원하는 프로그램을 구현했다. 처음에는 `load_from_file()`이 파일 읽기뿐 아니라 상품 종류 판단과 객체 생성까지 모두 담당하고 있었다.

### 문제
`load_from_file()`의 역할을 줄이기 위해 JSON 딕셔너리를 상품 객체로 변환하는 `create_product_from_dict()`를 분리했다. 그러나 분리 과정에서 두 가지 오류가 발생했다.

- 이 함수를 `ProductManager`의 메서드처럼 호출하여 `AttributeError`가 발생했다.
- 이후 함수 이름으로 직접 호출했지만, 함수 정의 위치가 적절하지 않아 `NameError`도 발생했다.

### 내가 한 행동

**원인 파악**
- 오류 메시지에서 `ProductManager` 객체 또는 클래스에 해당 속성이 없다는 내용을 확인했다.
- `create_product_from_dict()`가 `self`를 사용하는 인스턴스 메서드가 아니라, 딕셔너리 하나를 받아 객체 하나를 반환하는 일반 함수라는 점을 다시 구분했다.

**수정**
- 함수를 상품 클래스 정의 아래, `ProductManager` 정의 위에 배치했다.
- `ProductManager.create_product_from_dict()`나 `manager.create_product_from_dict()`가 아니라 함수 이름으로 직접 호출했다.
- `load_from_file()` 내부의 클래스 선택용 `if`·`elif`를 제거하고, 생성 함수의 반환 객체를 등록하도록 수정했다.

**검증**
- 개별 함수 테스트와 전체 JSON 저장·복원 테스트를 다시 실행했다.

### 결과

**동작**
- `create_product_from_dict()`가 다운로드 상품 딕셔너리를 실제 `DownloadProduct` 객체로 정상 변환했다.
- 저장 전과 복원 후에 상품 종류와 추가 속성이 동일하게 유지됐다.

**구조**
- `load_from_file()`은 파일 읽기와 등록을, 별도 함수는 상품 종류 판단과 객체 생성을 담당하도록 역할이 분리됐다.
- 일반 함수와 인스턴스 메서드의 정의 위치 및 호출 방식 차이를 실제 오류를 통해 확인했다.

### 배운 점
- 기능이 정상 동작하는 것뿐 아니라, 함수가 어떤 객체의 상태를 필요로 하는지를 기준으로 일반 함수와 인스턴스 메서드를 구분해야 한다.
- 한 함수가 여러 역할을 담당할 때 객체 생성처럼 독립적인 책임을 분리하면 코드의 역할과 수정 위치가 더 명확해진다.

### 활용 가능성
- 자소서 핵심 경험으로 사용하기에는 아직 규모가 작다.
- 학습 과정의 문제 해결 경험이나, 면접에서 최근 오류를 분석한 사례로 활용할 수 있다.
- 이후 더 큰 프로젝트에서 역할 분리나 리팩터링 경험이 쌓이면 함께 묶을 수 있다.

---

### 면접 답변 (선택)

**Q. 최근에 겪은 오류를 하나 설명해 주세요.**

JSON으로 저장한 상품 정보를 다시 객체로 복원하는 프로그램을 만들었는데, `load_from_file()` 하나가 파일 읽기와 상품 종류 판단, 객체 생성을 모두 담당하고 있었습니다. 객체 생성 부분을 `create_product_from_dict()`로 분리했는데, 이 함수를 `ProductManager`의 메서드처럼 호출해서 `AttributeError`가 났습니다.

오류 메시지에서 `ProductManager`에 해당 속성이 없다는 내용을 확인했고, 이 함수는 `self`를 쓰지 않고 딕셔너리 하나를 받아 객체 하나를 반환하는 일반 함수라는 점을 다시 구분했습니다. 함수 이름으로 직접 호출했더니 이번에는 `NameError`가 났는데, 정의 위치 문제여서 상품 클래스 아래, `ProductManager` 위로 옮겨 해결했습니다.

결과적으로 `load_from_file()`은 파일 읽기와 등록만, 분리한 함수는 상품 종류 판단과 객체 생성만 담당하게 됐습니다.

**Q. 그 경험에서 무엇을 배웠나요?**

동작 여부만 보고 넘어가지 않고, 그 함수가 어떤 객체의 상태를 필요로 하는지를 기준으로 일반 함수와 인스턴스 메서드를 구분해야 한다는 것을 배웠습니다. 그리고 한 함수가 여러 역할을 맡고 있을 때 객체 생성처럼 독립적인 책임을 떼어내면, 나중에 어디를 고쳐야 하는지가 더 명확해진다는 것도 확인했습니다.


## 2026-08-02 경험 후보 - 예외 발생 후 객체 상태까지 확인한 JSON 불러오기 수정

> **한 줄 요약** — 예외가 발생했을 때 함수는 종료되었지만 기존 상품 목록이 그대로 남는 문제를 발견하고, `load()`가 시작될 때 목록을 먼저 비우도록 수정했다. 실패 원인이 서로 다른 경우는 `False`와 `ValueError`로 구분되도록 인터페이스도 함께 고쳤다.

## 상황

파이썬 객체지향 복습에서 일반 상품과 다운로드 상품을 관리하는 프로그램을 독립적으로 구현했다.

상품 객체를 딕셔너리로 변환해 JSON 파일에 저장하고, 새로운 관리자 객체에서 다시 불러와 원래 클래스 객체로 복원하는 기능을 만들었다. 정상적인 파일을 저장하고 불러오는 기능뿐 아니라, 파일이 없거나 JSON 형식이 잘못된 경우도 처리해야 했다.

## 문제

**불러오기**

처음에는 파일을 읽는 과정에서 `FileNotFoundError`나 `JSONDecodeError`가 발생하면 `return`으로 함수를 종료하도록 작성했다. 하지만 기존 상품이 들어 있는 관리자 객체에서 존재하지 않는 파일이나 잘못된 JSON 파일을 불러오자, 함수는 종료되었지만 이전 상품 목록이 그대로 남았다.

**가격 변경**

상품이 없는 경우와 가격이 잘못된 경우를 모두 `False`로 처리하여, 호출하는 쪽에서 실패 원인을 구분할 수 없었다.

## 원인

**불러오기**

함수에서 값을 반환하거나 실행을 종료하는 것과, 객체의 내부 상태를 변경하는 것을 같은 동작처럼 생각한 것이 원인이었다. `return`이나 `return []`을 실행해도 함수가 끝나거나 값이 반환될 뿐, `self.products`가 자동으로 빈 리스트로 변경되는 것은 아니었다.

**가격 변경**

서로 다른 실패 상황을 하나의 반환값으로 처리하여 정보가 사라졌다.

- 상품을 찾지 못함
- 상품은 존재하지만 가격이 잘못됨

## 시도

**불러오기**

기존 상품이 들어 있는 관리자 객체를 만든 뒤 존재하지 않는 파일을 불러오고, 전체 조회 결과를 확인했다. 잘못된 JSON 파일도 직접 만들어 같은 방식으로 실행했다.

**가격 변경**

메서드의 반환값과 검색된 객체의 실제 가격을 함께 출력했다. 음수 가격을 전달해 프로퍼티 setter의 `ValueError`가 어디까지 전달되는지도 확인했다.

## 해결

**불러오기**

`load()`가 시작될 때 기존 상품 목록을 먼저 비우도록 수정했다.

```python
def load(self, filename):
    self.products = []

    try:
        with open(filename, "r", encoding="utf-8") as file:
            products_dict = json.load(file)

    except FileNotFoundError:
        return

    except json.JSONDecodeError:
        return
```

이후 파일 읽기에 실패하더라도 기존 상품이 남지 않고 빈 상태로 시작했다.

**가격 변경**

상품을 찾지 못한 경우에만 `False`를 반환하고, 잘못된 가격은 setter의 `ValueError`가 호출자에게 전달되도록 수정했다.

```python
def update_price(self, name, new_price):
    product = self.search_product(name)

    if product is None:
        return False

    product.price = new_price
    return True
```

## 결과

다음 상황을 각각 구분하여 확인했다.

**불러오기**

| 상황 | 결과 |
| --- | --- |
| 정상 JSON 불러오기 | 상품 객체 복원 |
| 존재하지 않는 파일 | 빈 상품 목록 |
| 잘못된 JSON | 빈 상품 목록 |
| 복원된 상품 삭제 | 정상 처리 |

**가격 변경**

| 상황 | 결과 |
| --- | --- |
| 정상 가격 변경 | `True` 반환 및 실제 가격 변경 |
| 상품이 없음 | `False` 반환 |
| 음수 가격 | `ValueError` 발생 |

## 배운 점

- **정상 입력만으로는 부족하다** — 정상적인 입력에서 프로그램이 실행되는지만 확인해서는 객체의 상태 문제를 발견하기 어렵다.
- **예외 이후의 상태도 설계 대상이다** — 예외가 발생했을 때 함수가 어떤 값을 반환하는지뿐 아니라, 기존 객체의 내부 상태를 유지할지 초기화할지도 설계해야 한다.
- **실패 원인이 다르면 알리는 방법도 달라야 한다** — 서로 다른 실패 원인을 같은 반환값으로 합치기보다, 반환값과 예외를 구분하여 호출자가 원인을 판단할 수 있게 해야 한다.

## 활용 가능성

이 경험은 자기소개서나 면접에서 다음 주제에 활용할 수 있다.

- 정상 동작 이후 예외 상황까지 테스트한 경험
- 반환값과 객체 상태 변경의 차이를 발견한 경험
- 실패 원인을 구분하도록 인터페이스를 수정한 경험
- 직접 테스트 데이터를 만들어 원인을 확인한 경험


## 2026-08-03 경험 후보 -유사한 코드를 재사용하다 달라진 조건을 놓친 오류를 수정한 경험

### 한 줄 요약
대여 데이터 필터링 문제를 연속으로 풀면서 직전 문제의 조건을 그대로 사용해 합계가 63000원으로 잘못 나왔고, `결과에 넣을 값`과 `선택 조건`을 다시 나누어 `if not is_returned`로 고쳐 48000원을 얻었다.

### 상황
대여 데이터를 이용해 두 가지 필터링 문제를 연속으로 해결했다.

- 첫 번째 문제: `반납 완료이면서 대여료가 25000원 이상인 물품` 찾기
- 두 번째 문제: `미반납 물품의 대여료 합계` 구하기

두 문제 모두 `rental_totals`와 `returned`를 `zip()`으로 묶어 처리하는 구조가 비슷했다.

### 문제
두 번째 문제에서도 직전 문제의 조건을 그대로 사용했다.

```python
if is_returned and rental_total >= 25000
```

그 결과 미반납 물품이 아니라, 반납을 완료한 고액 물품이 선택됐다.

| 구분 | 선택된 대여료 | 합계 |
| --- | --- | --- |
| 잘못된 결과 | `[36000, 27000]` | 63000원 |
| 기대한 결과 | `[28000, 20000]` | 48000원 |

### 확인과 수정

**확인한 것**

실제 결과와 문제에서 요구한 결과를 비교한 뒤, 조건을 다시 분리했다.

- 결과에 넣을 값: 미반납 물품의 대여료
- 선택 조건: 반납 여부가 `False`
- 필요하지 않은 조건: 대여료 25000원 이상

여기서 25000원 조건은 방향을 바꾸는 것이 아니라 아예 지워야 했다. 기대한 결과에 20000원 물품이 포함되어 있어, 금액 조건을 남겨두면 그 값이 빠지기 때문이다.

**수정한 조건**

```python
if not is_returned
```

**수정 후 결과**

- `[28000, 20000]`
- 미반납 대여료 합계: 48000원

### 배운 점
코드 구조가 비슷하더라도 선택 조건까지 같은 것은 아니다.
기존 코드를 재사용할 때는 먼저 현재 문제를 다음 두 부분으로 나누어 확인해야 한다.

1. 결과에 어떤 값을 넣을 것인가
2. 어떤 조건의 항목을 선택할 것인가

또한 예상 결과와 실제 결과가 다르면 계산식만 확인하지 않고, 직전 문제의 조건이 남아 있는지도 점검해야 한다.

### 활용 가능성
`실수했던 경험`, `문제를 발견하고 해결한 과정`을 묻는 질문에 쓸 수 있다.
유사한 코드를 재사용할 때 구조와 요구 조건을 분리해서 검토한 사례, 예상 결과와 실제 결과의 차이로 오류 원인을 확인한 사례로 활용할 수 있다.
현재는 초기 학습 단계의 보조 사례로 보관하며, 이후 더 큰 프로젝트 문제 해결 경험이 생기면 주력 경험에서는 교체한다.


## 2026-08-04 경험 후보 - 서버 점검 generator에서 반복 대상과 조건 판단 오류를 수정한 경험

### 한 줄 요약

서버 점검 기록을 generator로 처리하다 오류가 났는데, 정리해 보니 세 건의 원인이 하나였다. 서버 점검 기록을 generator로 처리하는 독립 재현에서 반복 변수 `check`를 현재 서버 기록이 아니라 iterator처럼 잘못 다뤘고, 출력과 조건식에서도 현재 요소 대신 고정값과 기준값을 사용해 오류가 발생했다.
### 상황

- iterable, iterator, generator의 기본 동작을 학습한 뒤 서버 점검 기록을 새로운 문제로 적용했다.
- iterator로 점검 기록을 한 건씩 처리하고, generator로 응답 시간이 기준 이상인 서버만 생성해야 했다.

### 문제

처음 작성한 generator 코드에서 오류가 발생했다.

**단순 오타 (1건)**

- `yield`를 `yeild`로 잘못 작성해 `SyntaxError`가 발생했다.

**반복 대상과 조건 판단 오류 (3건)**

- `for check in checks`로 이미 딕셔너리 한 건을 꺼냈는데 다시 `next(check)`를 호출했다. → `check`를 iterator로 봤다.
- 현재 서버 이름 대신 `"인증 서버"`를 고정 출력했다. → 현재 요소 대신 고정값을 썼다.
- `check["response_time"]`과 `standard`를 비교해야 했는데 `standard >= 300`으로 작성했다. → 현재 데이터 대신 기준값만 검사했다.

### 확인 과정

1. 오류가 발생한 줄을 확인했다.
2. 반복문 안의 `check`에 실제로 어떤 값이 들어오는지 다시 확인했다. `check`는 iterator가 아니라 이미 꺼낸 서버 기록 딕셔너리였다.
3. 조건식이 현재 기록의 응답 시간을 검사하지 않고 기준값 자체만 검사하고 있다는 점을 구분했다.

### 수정

- `yeild` → `yield`
- `next(check)` → 제거
- `"인증 서버"` 고정 출력 → `check["server"]`
- `standard >= 300` → `check["response_time"] >= standard`

### 결과

- 응답 시간이 300ms 이상인 결제 서버와 로그 서버만 생성됐다.
- 첫 번째 느린 서버는 `next()`로 처리하고, 남은 느린 서버는 같은 generator를 `for`문으로 이어서 처리했다.
- 다만 처음부터 독립적으로 완성한 것은 아니다. 오류 원인과 수정 방향을 확인한 뒤 정상 실행까지 완료했다.

### 배운 기준

- 세 오류는 모두 반복 변수 `check`의 역할을 정확히 사용하지 못한 데서 나왔다. `check`는 이미 현재 서버 기록 딕셔너리인데 iterator처럼 다뤘고, 출력과 조건식에서는 현재 요소 대신 고정값과 기준값만 사용했다.
- `for 변수 in iterable`에서 변수에는 이미 현재 요소가 들어온다. 그 변수에 다시 `next()`를 쓰지 않는다.
- 조건식에는 기준값뿐 아니라 실제로 검사하려는 현재 데이터가 포함되어야 한다.
- generator는 같은 객체를 계속 사용하면 이전 `yield`의 실행 상태를 이어서 처리한다.

### 면접에서 말할 수 있게 정리

- **30초 버전**
  서버 점검 기록에서 응답 시간이 기준 이상인 서버만 generator로 생성하는 코드를 작성했는데 오류가 났다. `for`문으로 이미 꺼낸 딕셔너리에 `next()`를 다시 썼고, 서버 이름은 고정값으로 출력했고, 조건식은 기준값 자체만 검사하고 있었다. 반복 변수에 실제로 무엇이 들어오는지 다시 확인해서 세 곳을 모두 현재 요소 기준으로 고쳤고, 응답 시간이 300ms 이상인 결제 서버와 로그 서버만 생성되는 것을 확인했다.

- **"원인이 뭐였나요?"**
  `check`가 이미 현재 서버 기록 딕셔너리라는 사실을 놓치고 iterator처럼 다뤘으며, 출력과 조건식에서도 현재 요소 대신 고정값과 기준값만 사용한 것이 원인이었습니다.

- **"혼자 해결했나요?"**
  처음부터 독립적으로 완성하지는 못했다. 오류 원인과 수정 방향을 확인한 뒤 정상 실행까지 완료했다.

- **"다음에는 어떻게 하나요?"**
  `for 변수 in iterable`을 작성한 직후 반복 변수에 실제로 어떤 자료형과 값이 들어오는지 먼저 확인하고, 조건식의 왼쪽에는 현재 데이터가, 오른쪽에는 비교 기준이 들어갔는지 점검한다.


## 2026-08-05 경험 후보 - 출력 결과와 구현 요구사항을 분리해 검증한 경험

### 한 줄 요약

출력은 맞았지만 요구사항대로 구현되지 않은 코드를 발견하고, 요구사항과 코드를 직접 대조하는 검증 순서를 만든 경험이다.

### 상황

함수 객체와 `filter()`, `map()`을 학습한 뒤 점수 처리 프로그램을 독립 재현했다. 이 경험과 직접 관련된 핵심 요구사항은 다음 두 가지였다.

- 선택한 보너스 함수 객체를 변수에 저장할 것
- 60점 이상인 학생만 골라 해당 함수를 적용할 것

### 문제

처음 작성한 프로그램은 예상한 결과를 그대로 출력했다.

```python
mapped = map(
    lambda student: apply_bonus(
        student,
        select_bonus("2"),
    ),
    filtered_scores,
)
```

그러나 완료 조건과 코드를 다시 대조하자, 선택한 함수 객체를 변수에 저장하지 않고 `map()` 내부에서 `select_bonus("2")`를 반복 호출하고 있었다. 출력값은 맞았지만 다음 요구사항은 충족하지 못한 상태였다.

```
함수 객체 선택 → 변수에 저장 → 저장한 함수 객체를 map에 전달
```

### 원인

프로그램의 최종 출력만 확인하고, 각 요구사항이 코드의 어느 부분에서 구현됐는지는 따로 확인하지 않았다. 그 결과 함수 선택과 함수 적용을 분리해야 한다는 구조적 조건을 놓쳤다.

### 수정

선택한 함수 객체를 먼저 변수에 저장한 뒤, 그 변수를 `map()`에 전달하도록 수정했다.

```python
selected_bonus = select_bonus("2")

mapped = map(
    lambda student: apply_bonus(student, selected_bonus),
    filtered_scores,
)
```

수정 후에는 다음 조건을 코드와 실행 결과로 하나씩 다시 확인했다.

- `selected_bonus`에 함수 객체가 저장됐는가
- `filter()`가 60점 이상만 통과시켰는가
- `map()`이 통과한 학생만 변환했는가
- 원본 점수 목록이 유지됐는가
- 소진된 `map` 객체의 재사용 결과가 `[]`인가

### 결과

수정 후에도 결과는 그대로 출력됐다.

```
지수 80 → 90
현우 67 → 77
서연 92 → 100
```

원본 점수 목록은 변경되지 않았고, 소진된 `map` 객체는 `[]`가 됐다. 최종적으로 출력 결과와 구현 요구사항을 모두 충족했다.

### 배운 점

프로그램의 결과가 맞는 것과 요구사항에 맞는 구조로 구현한 것은 별개의 문제다. 함수의 저장 위치, 역할 분리, 원본 보존처럼 실행 결과만으로 확인할 수 없는 조건은 요구사항과 코드를 직접 대조해야 한다.

앞으로는 다음 순서로 검증한다.

1. 실행 결과 확인
2. 요구사항별 구현 위치 확인
3. 원본 데이터와 객체 상태 확인
4. 완료 조건 최종 대조

### 활용 가능 범위

- 결과는 맞았지만 구현상의 누락을 발견한 경험
- 요구사항을 기준으로 코드를 다시 검증한 경험
- 자신의 검증 방식을 개선한 경험


## 2026-08-06 경험 후보 - 데코레이터의 입력 시점을 구분해 주문 처리 코드를 재구현한 경험

## 한 줄 요약

데코레이터의 바깥 함수가 주문 딕셔너리를 받는다고 잘못 알아 `TypeError`가 발생했다.
원래 함수 객체를 받는 시점과 실제 데이터를 받는 시점을 나누어 프로그램을 다시 작성했다.

## 경험 유형

- 새로운 개념 학습
- 오류 원인 확인
- 기능 분리
- 피드백 반영

## 상황

클로저와 데코레이터를 학습한 뒤, 검사·로그·주문 계산·성공 횟수 누적 기능을 결합한 주문 처리 프로그램의 독립 재현을 시도했다.

## 문제

**1) 데코레이터가 값을 받는 시점을 잘못 판단했다**

데코레이터의 바깥 함수가 실제 주문 딕셔너리를 받는다고 잘못 판단했다.
그 결과 함수 객체에 딕셔너리처럼 접근하여 다음 오류가 발생했다.

```text
TypeError: 'function' object is not subscriptable
```

**2) 처리와 기록의 역할이 섞였다**

성공 횟수를 기록하는 함수 안에서 주문 처리 함수를 다시 호출하려 하여, 처리와 기록의 역할도 섞였다.

## 행동

1차 독립 재현에서는 오류를 스스로 해결하지 못했다.
설명을 받은 뒤 다음과 같이 구조를 다시 나누었다.

- 데코레이터 바깥 함수는 원래 함수 객체를 받도록 수정했다.
- `wrapper`가 실제 주문 데이터를 받도록 분리했다.
- 검사 기능만 먼저 실행한 뒤 로그 기능을 연결했다.
- 주문 계산 함수는 주문을 처리하고, 성공 횟수 클로저는 처리 결과만 기록하도록 역할을 분리했다.
- 정상 주문과 실패 주문을 차례로 실행하여 반환값과 누적 횟수를 확인했다.

## 결과

보조 설명 후 프로그램을 재구현하여 다음을 확인했다.

- 정상 주문은 계산되고 성공 횟수가 증가했다.
- 잘못된 주문은 검사 단계에서 차단되고 성공 횟수가 유지됐다.
- 원본 주문 딕셔너리는 변경되지 않았다.
- 로그와 검사 데코레이터가 의도한 순서로 실행됐다.

책, 키보드, 마우스 주문을 차례로 처리하여 성공 횟수가 1 → 1 → 2로 누적되는 것을 확인했다.

수행 결과는 다음과 같다.

- 1차 독립 재현: 실패
- 보조 설명 후 재구현: 성공
- 최종 프로그램 실행: 성공

## 배운 점

**데코레이터 구조에서는 원래 함수 객체와 실제 호출 데이터를 서로 다른 시점에 받는다.**

- 데코레이터 적용 시점 → 바깥 함수가 원래 함수 객체를 받는다.
- 실제 함수 호출 시점 → `wrapper`가 실제 데이터를 받는다.

`@decorator`가 `function = decorator(function)`과 같기 때문이다.

**오류가 난 코드를 한 번에 고치지 않는다.**

여러 기능이 결합된 코드에서 오류가 발생하면 전체 코드를 동시에 수정하기보다, 검사·처리·기록 기능을 분리하고 각 단계의 입력값과 반환값을 확인해야 한다는 점을 배웠다.

## 현재 활용 가능성

데코레이터를 독립적으로 설계한 경험은 아니다.
다만 다음 주제의 보조 경험으로 활용할 수 있다.

- 새로운 개념을 잘못 이해한 뒤 수정한 경험
- 피드백을 반영해 코드를 재구현한 경험
- 여러 기능의 역할을 분리한 경험
- 실패한 독립 재현을 기록하고 검증한 경험

## 꼬리 질문 대비

| 질문 | 답의 핵심 |
| --- | --- |
| 무슨 오류였나 | 함수 객체를 받는 자리에 딕셔너리처럼 접근해서 `'function' object is not subscriptable`이 발생했다 |
| 왜 `wrapper`가 필요한가 | 원래 함수 객체를 받는 시점과 실제 데이터를 받는 시점이 달라서, `wrapper`가 그 사이를 연결한다 |
| 데코레이터를 겹치면 실행 순서는 | 바깥쪽부터 실행된다. 로그 → 검사 → 원래 계산 함수 순이었다 |
| 스스로 해결했나 | 1차 독립 재현은 실패했고, 설명을 받은 뒤 재구현해서 성공했다 |


## 2026-08-07 경험 후보 - 예제에 맞춘 구현을 요구사항 기준으로 고친 경험

### 한 줄 요약
예제 출력만 보고 짠 코드가 실행 결과는 맞았지만 요구사항은 만족하지 못한다는 것을 발견하고, 입력 조건을 기준으로 동작하도록 고친 경험이다.

### 상황
파이썬 특수 메서드를 학습한 뒤 `NumberSequence` 클래스의 독립 재현을 시도했다.
`start`, `final`, `step`만 저장하고 별도의 값 목록은 만들지 않은 채, 반복과 길이 계산이 모두 가능해야 한다는 조건이었다.

### 문제
처음에는 예제 결과가 `2, 4, 6, 8, 10`인 것을 보고 `__iter__()`에 짝수만 통과시키는 조건을 넣었다.
실행 결과는 맞았지만, 요구사항에는 없는 규칙을 예제만 보고 추가한 구현이었다.
`__len__()`에서도 값을 모두 `list`로 만든 뒤 길이를 구해, 값을 미리 만들지 않는다는 조건을 만족하지 못했다.

### 수정
예제의 숫자가 아니라 `start`, `final`, `step`이 실제 규칙이라는 점을 다시 확인했다.
짝수 조건을 제거하고 `range`에서 `step`에 따라 값을 생성하도록 수정했다.
이후 `range` 자체가 iterable이므로 `iter(range(...))`를 바로 반환할 수도 있다는 더 간단한 방식을 확인했다.
길이는 먼저 `start`, `final`, `step`만으로 직접 계산하도록 고쳤고, 이후 `len(range(...))`으로 같은 결과를 더 간단하게 얻을 수 있다는 것을 확인해 그 방식으로 정리했다. 불필요한 `list` 생성도 함께 없앴다.

### 결과
예제에만 맞는 코드에서 벗어나 입력 조건을 기준으로 동작하는 코드로 수정했다.
또한 이미 반복과 길이 계산에 필요한 기능을 제공하는 `range`를 다른 자료구조로 변환하지 않고 그대로 사용할 수 있게 되었다.

### 배운 점
예제 출력이 맞는 것과 요구사항을 올바르게 구현한 것은 다르다.
두 실수 모두 결과가 예제와 같은지에만 집중하다 생긴 것이었다.
구현 전에 예제에서 우연히 보이는 패턴과, 입력이 달라져도 유지되어야 하는 실제 규칙을 구분해야 한다는 기준을 얻었다.

### 예상 꼬리 질문
- **왜 짝수 조건을 넣었나?** → 예제 출력이 `2, 4, 6, 8, 10`이어서 그 숫자만 보고 규칙을 판단했다고 답한다.
- **잘못된 것을 어떻게 알았나?** → 구현 결과를 확인받는 과정에서, 짝수 조건은 요구사항에 없는 조건이고 `list()` 생성 역시 값을 미리 만들지 않는다는 조건에 어긋난다는 피드백을 받아 확인했다.
- **지금이라면 어떻게 접근하겠나?** → 예제부터 코드로 옮기지 않고, 요구사항에 적힌 조건을 먼저 구현한 뒤 예제로 검증하겠다고 답한다.

### 활용 포인트
- 결과만 맞는 코드의 문제를 발견하고 요구사항 기준으로 수정한 경험
- 예제에 과적합된 조건을 제거하고 일반화한 경험
- 불필요한 중간 자료구조 생성을 줄인 경험


## 2026-08-08 경험 후보 — 개별 문법을 하나의 프로그램으로 연결하며 막힌 지점 해결

**한 줄 요약:** 문법은 각각 알고 있었지만 하나로 연결하지 못했고, 막힌 지점을 단계별로 나눠 고치면서 `StudyRecord` 객체를 실제 파일로 저장하는 프로그램을 완성했다.

### 상황
`dataclass`, 패키지, `pathlib`을 함께 사용해 학습 기록 객체를 파일로 저장하는 프로그램을 구현했다.

### 문제
각 문법은 개별 실습에서는 쓸 수 있었지만, 요구사항만 보고 객체 생성·패키지 구조·저장 함수·파일 경로를 하나의 프로그램으로 연결하지 못했다.
특히 `StudyRecord`의 데이터를 `save_record()`가 받아 실제 파일로 저장하는 전체 흐름을 처음부터 구성하는 데서 막혔다.

### 행동
- **쓸 수 있는 데까지 먼저 쓰기** — 내가 쓸 수 있는 만큼, `StudyRecord`와 `save_record()`의 기본 구조까지는 직접 작성했다.
- **막힌 지점 쪼개기** — 막힌 부분을 클래스 선언, 파일 경로, 문자열 조립, 패키지 import, 실행 흐름으로 나누어 하나씩 수정했다.
- **역할 나누기** — `record.py`, `storage.py`, `__init__.py`, 실행 파일로 역할을 분리했다.
- **결과까지 확인하기** — 프로그램 실행 결과만 보지 않고, `record.txt`의 실제 내용까지 직접 열어서 확인했다.

### 결과
`StudyRecord("pathlib", 45)` 객체의 정보를 실제 파일로 저장하는 프로그램을 완성했다. `record.txt`에 저장된 내용은 다음과 같다.

```
주제: pathlib
시간: 45분
태그: python, file
```

다만 전체 구조를 처음부터 독립적으로 완성한 것은 아니다. 막힌 부분은 설명을 받은 뒤 수정하면서 완성했다.

### 배운 점
개별 문법을 아는 것과, 여러 기능의 역할을 나누어 하나의 프로그램으로 구성하는 것은 별개의 능력이라는 점을 확인했다.
통합 문제에서는 바로 코드를 쓰기보다 `데이터 → 처리 → 저장 → 실행` 순서로 역할을 먼저 나누는 것이 필요하다는 기준을 얻었다.

### 활용 후보
- 여러 기능을 하나의 프로그램으로 연결하며 문제를 분해한 경험 → "문제가 막혔을 때 어떻게 해결하나요"
- 모듈별 역할을 분리하고 실제 파일 출력까지 검증한 경험 → "결과가 맞는지 어떻게 확인하나요"
- 도움받은 부분과 직접 해결한 부분을 구분해 개선한 경험 → "본인의 부족한 점은 무엇인가요"


## 2026-08-09 경험 후보 — 여러 개념을 조합했을 때 막힌 지점을 분해한 경험

### 한 줄 요약

개별 개념 문제는 풀 수 있었지만, 여러 개념을 하나로 조합하자 코드를 독립적으로 짜지 못했다.
막힌 지점을 세 개로 나누어 다시 확인했고, **해결된 것과 아직 아닌 것을 구분해서 기록했다.**

---

### 상황과 문제

5주차 복습에서 `dataclass`, generator, `map`, decorator, module/package를 하나의 프로그램으로 조합해 독립 재현을 시도했다.
개별 개념 문제는 풀 수 있었지만, 여러 개념을 동시에 사용하자 코드 구조를 독립적으로 만들지 못했다.

특히 세 군데에서 막혔다.

1. 여러 객체가 담긴 collection과 객체 하나를 구분하지 못했다. (`records`에 `records.minutes`처럼 접근했다)
2. `map()`에서 값과 함수 중 무엇이 필요한지 판단하지 못했다.
3. decorator에서 `wrapper`와 원래 함수의 인수를 연결하지 못했다.

---

### 행동

- 먼저 독립 재현 실패를 그대로 기록했다. **답을 보고 수정한 부분은 성공으로 처리하지 않았다.**
- 그다음 실패 원인을 작은 문제로 나누어 `Product` 예제로 다시 확인했다.
  1. `products → product → product.price` 구조를 판단한 뒤 generator를 직접 작성했다.
  2. `map(lambda product: product.name, products)`를 직접 작성했다.
  3. decorator는 단계적인 설명을 받은 뒤, 인수 전달과 반환값 보존 구조를 작성하고 실행 결과를 확인했다.

---

### 결과

- collection과 요소 하나의 구분, `map()`에서 함수와 데이터의 역할은 `Product` 예제에서 직접 적용할 수 있었다.
- 반면 decorator는 도움을 받은 뒤 작성했으므로, 독립 재현이 가능하다고 판단하지 않았다.

---

### 얻은 기준

코드를 작성하기 전에 다음 세 가지를 확인한다.

1. 현재 변수는 하나인가, 여러 개인가?
2. 변수에는 실제로 무엇이 들어 있는가?
3. 현재 자리에 필요한 것은 값인가, 함수인가?

---

### 이 경험에서 드러나는 점

- 정답을 맞히는 것으로 끝내지 않고, 실패 지점을 분리해 새 예제에서 다시 검증했다.
- 성공과 미성공의 기준을 스스로 정하고, 그 기준대로 기록했다.
- 한 번의 해결로 끝내지 않고, 다음에 재사용할 판단 기준으로 정리했다.

---

### 예상 질문

1. **가장 막혔던 부분은 어디였나요?**
   → collection과 요소 하나를 구분하지 못한 부분. `records`에 `records.minutes`처럼 접근했다.
2. **decorator는 결국 해결하지 못한 것 아닌가요?**
   → 설명을 받은 뒤 작성했고 실행 결과까지 확인했지만, 도움을 받았기 때문에 독립 재현으로 처리하지 않았다.
3. **그 기준이 실제로 도움이 됐나요?**
   → `Product` 예제로 나누어 확인했을 때, generator와 `map()`을 직접 작성할 수 있었다.


## 2026-08-10 경험 후보 - 코드 연결 판단 실패 원인 분해

## 한 줄 요약

문법이 아니라 **코드 사이의 연결 판단**에서 막힌다는 것을 확인했다.
막힌 지점을 항목별로 쪼개서, 어디까지 혼자 되고 어디부터 안 되는지를 구분했다.

---

## 상황

Python 핵심 진도와 복습을 마친 뒤 통합 독립 재현을 진행했다.
여러 함수와 decorator가 연결된 구조를, 요구사항만 보고 처음부터 구성하지 못했다.

---

## 문제

문법 자체보다 다음 연결 판단에서 막혔다.

1. collection 전체와 요소 하나 구분
2. 변수에 현재 들어 있는 값 추적
3. `func`와 `func()` 선택
4. 매개변수의 실제 입력을 호출부에서 역추적
5. 인수 있는 decorator에서 `decorator 인수 → 원래 함수 → wrapper → 실제 호출값` 연결 설계

---

## 내가 한 행동

문제를 한 번에 다시 풀기보다, 연결 판단을 항목별로 나눠 확인했다.

- **변수 추적** — 이름이 아니라 대입 위치와 호출부를 따라 실제 값을 추적했다.
- **`func` / `func()` 판단** — "지금 필요한 것이 함수 자체인지 실행 결과인지"를 기준으로 판단했다.
- **decorator 해석** — `@decorator(x)`를 `decorator(x)(원래 함수)`로 풀어, 각 단계에 무엇이 전달되는지 확인했다.
- **decorator 구현** — 검사 함수, 원래 함수, 실제 호출값이 각각 다른 단계에서 전달된다는 점을 기준으로 수정했다.

---

## 결과

**독립적으로 수행한 것**

- collection/요소 구분, 변수 추적, `func`/`func()` 판단, 함수 인수 역추적
  → 개별 문제에서 독립적으로 수행했다.

**안내를 받고 수정한 것**

- 인수 있는 decorator 직접 구성
  → 첫 시도에 실패했고, 안내를 받은 뒤 수정했다.

**독립 성공으로 처리하지 않은 것**

- 최종 통합 독립 재현
  → 외부 AI 도움을 받아 완성했으므로 독립 성공으로 처리하지 않았다.

---

## 얻은 기준

여러 함수가 연결된 코드를 작성할 때는, 구현하기 전에 다음 5가지를 먼저 확인한다.

1. 무엇이 들어오는가
2. 지금 값인가, 함수인가
3. 어디에서 호출되는가
4. 무엇을 반환하는가
5. 그 반환값을 누가 쓰는가

> 한 줄로:
> `무엇이 들어오는가 → 지금 값인가 함수인가 → 어디에서 호출되는가 → 무엇을 반환하는가 → 그 반환값을 누가 쓰는가`

---

## 재검증 필요

- 요구사항만 보고 인수 있는 decorator 구조를 독립 설계하는 능력
- 여러 기능이 섞였을 때 전체 데이터 흐름을 처음부터 나누는 능력


## 2026-08-11 경험 후보 - 화면 결과가 아니라 HTML 구조를 기준으로 수정

### 한 줄 요약

화면에는 비슷하게 나왔지만 HTML 구조가 요구사항과 달랐던 프로필 페이지를, 각 요구사항이 어떤 element의 역할인지를 기준으로 다시 확인해 구조까지 맞게 수정했다.

---

### 상황

HTML/CSS 첫 학습에서 공통 카드 디자인, 문단 공통 class, 목록형 링크를 포함한 프로필 페이지를 구현했다.

### 문제

처음에는 화면에 비슷한 결과가 나왔지만, 요구사항과 HTML 구조가 일치하지 않았다.

- 카드 전체에 적용해야 할 class를 heading에 적용했다.
- 여러 문단에 같은 class를 지정해야 했지만, 부모 element의 상속으로 대신했다.
- 목록형 링크에서 `li`를 빠뜨렸다.

HTML/CSS를 처음 배우던 날이라, 요구사항을 어느 element의 역할로 옮길지 판단하는 기준 자체가 없었다. 그래서 왜 그렇게 붙였는지는 지금 다시 봐도 특별한 이유가 없다. 게다가 브라우저는 일부 잘못된 HTML도 화면에 표시해 주기 때문에, 화면만 확인해서는 이 문제들을 구별하기 어려웠다.

### 확인

요구사항을 다시 읽고, 각 스타일과 내용이 실제로 어느 element의 역할인지를 기준으로 확인했다. 없던 판단 기준을 이 단계에서 처음 정리한 셈이다.

- 카드 전체 → 문단을 감싸는 container
- 문단 공통 스타일 → 각 `p`에 지정하는 공통 class
- 목록형 링크 → `ul`(목록 전체) → `li`(항목) → `a`(링크)

### 수정

확인한 기준에 맞춰 세 가지를 그대로 고쳤다.

- 카드 class를 heading에서 실제 container로 옮겼다.
- 상속으로 대신했던 문단 스타일을 세 문단에 동일한 class로 각각 지정했다.
- 링크를 `ul → li → a` 구조로 다시 작성하고, 잘못된 종료 태그를 정리했다.

### 결과

수정 후 화면과 HTML 구조가 모두 요구사항에 맞게 동작했다.

이후 별도의 독립 재현에서는 새 HTML/CSS 파일에서 `section`, 공통 class, 특정 id, 목록·링크 중첩, Box Model을 처음부터 직접 연결해 요구사항을 충족했다.

### 배운 점

웹페이지가 원하는 모양으로 보이는 것과 요구사항에 맞는 구조로 구현된 것은 별개의 문제다. 이후에는 화면 결과뿐 아니라 각 요구사항이 실제로 어떤 element와 구조에 연결되어 있는지도 함께 확인해야 한다.

---

### 예상 질문

**Q. 처음에 왜 그렇게 판단했나요?**

> HTML/CSS를 처음 배우던 날이라, 요구사항을 어느 element의 역할로 옮길지 판단하는 기준 자체가 없었습니다. 그래서 화면이 비슷하게 보이는지를 기준으로 작성했다. <!--그래서 왜 그렇게 붙였는지는 지금 봐도 특별한 이유가 없고,(문장이 개인적으로 마음에 들어서 남겨둔다)--> 그 기준을 만든 게 이 경험의 수정 단계였습니다.


## 2026-08-12 경험 후보 - 화면 결과만 보고 판단하지 않고, CSS 동작을 직접 검증해 수정한 경험

### 한 줄 요약

링크 간격이 화면에서는 맞아 보여 `gap`이 적용된 것으로 판단했지만, 조건을 바꿔가며 검증해 HTML의 기본 동작임을 확인했고, 배치 대상과 직접 부모를 다시 잡아 요구사항을 모두 충족했다.

---

### 상황

Flexbox를 이용해 프로필과 활동 카드 페이지를 독립적으로 구현했다. 첫 제출에서는 링크 간격과 프로필 카드 스타일 일부가 요구사항과 맞지 않았다.

---

### 문제

1. **화면만 보고 판단한 문제** — 링크들이 화면에서 이미 조금 떨어져 보여 `gap`이 적용된 것으로 판단했다.
2. **구조를 잘못 판단한 문제** — HTML 구조가 중첩되면서 어떤 요소를 배치 대상으로 잡고 어디에 `display: flex`를 적용해야 하는지 혼동했다.

---

### 시도

1. 부모 요소에 `border`를 넣어 어떤 요소들이 같은 부모에 속하는지 확인했다.
2. `gap`을 제거하거나 `display: flex`를 추가하며 브라우저 결과를 비교했다.
3. 그 결과 화면에 보이던 공백은 HTML의 기본 동작이고, 내가 지정하려던 `gap`과는 다른 것임을 확인했다.
4. 이후 **배치 대상 → 직접 부모 → flex container** 순서로 구조를 다시 판단했다.
5. 누락된 요구사항은 완성 코드를 받지 않고 조건을 다시 확인해 직접 수정했다.

---

## 결과

누락된 조건을 직접 수정해 독립 재현 요구사항을 모두 충족했다. 이후 추가 문제에서도 중첩된 구조의 배치 대상과 직접 부모를 구분해 Flexbox 적용 위치를 판단할 수 있었다.

---

### 배운 점

- 화면이 원하는 모습과 비슷하다는 것만으로 코드가 의도대로 동작한다고 판단하면 안 된다.
- 레이아웃 문제에서는 먼저 배치 대상과 직접 부모를 확인하고, 예상과 실제 동작이 다르면 조건을 바꿔가며 원인을 검증해야 한다.

---

### 면접에서 말할 때 (말로 하는 버전)

> Flexbox로 프로필과 활동 카드 페이지를 혼자 구현한 적이 있습니다. 첫 제출에서 링크 간격이 요구사항과 맞지 않았는데, 화면에서는 링크들이 이미 조금 떨어져 보여서 `gap`이 적용된 줄 알았습니다.
>
> 그래서 부모 요소에 `border`를 넣어 어떤 요소들이 같은 부모에 속하는지 확인하고, `gap`을 빼거나 `display: flex`를 넣어보며 화면을 비교했습니다. 확인해 보니 그 공백은 HTML의 기본 동작이었고, 제가 지정하려던 `gap`과는 다른 것이었습니다.
>
> 이후에는 배치 대상 → 직접 부모 → flex container 순서로 구조를 다시 판단했고, 완성 코드를 받지 않고 요구사항을 모두 충족시켰습니다. 이 경험으로 화면이 비슷해 보인다고 해서 코드가 의도대로 동작한다고 판단하면 안 된다는 걸 배웠습니다.


## 2026-08-13 경험 후보 — Flexbox 적용 위치를 판단하는 기준을 세워 페이지 배치를 구현한 경험

**핵심 한 줄**
"배치 대상의 직접 부모를 flex container로 만든다"는 기준을 세웠고, Bootstrap에도 그대로 적용했다.

---

### 상황

Bootstrap과 Flexbox를 이용해 상단 메뉴, 카드 영역, 버튼이 있는 페이지를 구현했다.
배치는 Bootstrap class로, 카드 모양과 hover 효과는 직접 작성한 CSS로 나눠서 처리했다.

### 문제

처음에는 `d-flex` 같은 class 자체를 flex container라고 생각해서, 어떤 요소에 적용해야 하는지 판단하지 못했다.
그래서 배치하려는 요소가 아닌 곳에 Flexbox를 적용해 원하는 정렬이 나오지 않았다.
특히 버튼을 오른쪽으로 옮길 때, 버튼 자체를 flex container로 만드는 것과 버튼의 부모를 flex container로 만드는 것을 구분하지 못했다.

### 행동

배치할 때마다 다음 순서로 HTML 구조를 먼저 확인했다.

1. 움직이고 싶은 대상이 무엇인가
2. 그 대상의 직접 부모가 무엇인가
3. 그 부모를 flex container로 만든다

이 기준에 따라 Bootstrap의 `d-flex`, `justify-content-*`, `gap`을 필요한 부모 요소에만 적용했다.
마지막에는 같은 화면을 보지 않고, 다른 구조의 페이지도 처음부터 다시 구현했다.

### 결과

상단 양 끝 배치, 카드 배치, 버튼 오른쪽 정렬, 중첩 Flexbox를 직접 연결해 구현했다.
독립 재현에서는 padding 누락과 일부 HTML 구조 오류가 있었고, 피드백을 받은 뒤 원인을 확인해 수정했다. 핵심 배치 구조는 같은 화면을 보지 않고 스스로 판단해 완성했다.
핵심 배치 구조는 같은 화면을 보지 않고 스스로 판단해서 완성했다.

### 배운 점

CSS property를 외우는 것보다, 어떤 요소에 적용해야 하는지를 HTML 부모·자식 구조에서 먼저 판단하는 것이 중요하다는 것을 배웠다.
Bootstrap처럼 미리 만들어진 class를 쓰더라도 같은 원리가 그대로 적용된다는 것도 확인했다.

---

### 면접용 한 줄

> Flexbox가 적용될 위치를 찾지 못해 헤매다가, "배치 대상의 직접 부모를 flex container로 만든다"는 기준을 세웠습니다.
> 이 기준을 Bootstrap에도 그대로 적용해서 중첩된 페이지 배치를 직접 구현했습니다.

### 예상 꼬리질문

**Q. 왜 버튼이 아니라 버튼의 부모에 적용했나요?**
버튼을 움직이는 것은 버튼 자신이 아니라 버튼을 담고 있는 부모이기 때문입니다. 그래서 부모를 flex container로 만들었습니다.

**Q. Bootstrap을 쓰는데 직접 CSS는 왜 따로 작성했나요?**
배치는 Bootstrap class로 처리하고, 카드 모양과 hover 효과는 직접 CSS로 나눠서 작성했습니다.

**Q. 독립 재현에서 실수한 부분은 없었나요?**
`padding`을 빠뜨렸고 HTML 구조 오류도 있었습니다. 확인 후 수정했고, 핵심 배치 구조는 스스로 판단해서 완성했습니다.


## 2026-08-14 경험 후보 1순위 — CSS 적용 위치를 잘못 잡고 수정한 경험

### 30초로 말할 때

입력 영역 전체를 하나의 박스로 꾸미려다가 CSS class를 개별 `<input>`에 적용했다. 그래서 입력창마다 스타일이 따로 적용됐다. 입력창과 버튼을 모두 감싸는 바깥 `<div>`로 class를 옮겨 해결했다. 그 뒤로는 CSS를 쓰기 전에 꾸밀 대상과 그 직접 부모를 먼저 확인한다.

### 상황

Bootstrap과 직접 CSS를 사용해 입력 폼이 포함된 페이지를 만들었다.

### 문제

입력 영역 전체를 하나의 박스로 꾸미려고 했다. 그런데 CSS class를 각 `<input>`에 적용했다. 그래서 입력창마다 `box-shadow`와 `border-radius`가 따로 적용되어 의도한 화면과 다르게 나왔다.

### 원인

CSS property만 생각했고, 실제로 꾸미려는 대상이 어떤 요소인지 먼저 확인하지 않았다.

### 해결

이름 입력창, 오늘 목표 입력창, 저장 버튼을 모두 포함하는 바깥 `<div>`를 확인한 뒤 CSS class를 그 `<div>`로 옮겼다.

### 결과

입력 영역 전체가 하나의 박스로 꾸며졌고, 의도한 화면으로 수정할 수 있었다.

### 이후 적용

독립 재현에서 두 버튼을 오른쪽에 배치할 때도, 버튼들의 직접 부모를 찾아 그 부모에 Flexbox를 적용했다.

### 배운 점

CSS나 Flexbox를 적용할 때는 두 가지를 먼저 확인한다.

1. 무엇을 꾸미거나 배치할 것인가
2. 그 대상 또는 직접 부모가 어떤 요소인가

### 30초로 말할 때

입력 영역 전체를 꾸미려고 했는데 CSS를 개별 입력창에 적용해서 원하는 결과가 나오지 않았습니다. 그래서 전체를 감싸는 부모 요소에 CSS를 적용해 해결했습니다. 이 경험으로 CSS를 적용하기 전에 먼저 꾸밀 대상을 확인해야 한다는 것을 배웠습니다.

---

## 보조 후보 — 평균 계산의 예외 처리 *(한두 줄로만 기억한다)*

평균을 구하는 문제에서 `count == 0`은 확인했지만, 평균 계산 코드가 그 아래에서 계속 실행되도록 작성했다. 계산 코드를 `else` 안으로 옮겨 수정하면서, 조건을 검사하는 것뿐 아니라 조건 이후 어떤 코드가 실행되는지도 확인해야 한다는 점을 배웠다.

---

### 면접에서 답할 수 있어야 하는 것

- 그래서 뭘 잘못했나요? → **문제**
- 어떻게 고쳤나요? → **해결**
- 무엇을 배웠나요? → **배운 점**


## 2026-08-15 경험 후보 — CSS/Flexbox 적용 위치 판단 기준을 세운 경험

> **이 파일 사용법**: 외우는 문서가 아니다. 면접 전날 5분 훑어보는 문서다.
> 코드는 `f3_independent_reproduction.html`에 그대로 있으니, 같이 열어보면 세부 내용은 다시 떠오른다.

## 이것만 기억한다
1. 페이지 수정 중에 CSS를 어디에 적용할지 매번 헷갈렸다.
2. '꾸밀 대상 → 배치할 요소 → 직접 부모' 순서로 판단하는 기준을 세웠다.
3. 구조가 다른 페이지를 완성 코드 없이 다시 만들었고, Flexbox를 쓴 네 곳이 전부 배치할 요소의 직접 부모였다.

살을 붙일 때 쓰는 두 가지
- **요구사항 5개** = 꾸미기 2 + 배치 2 + 효과 1
- **함정 2개** = `input`이 아니라 감싸는 요소 / 텍스트가 아니라 카드 뭉치

숫자(1000px, 6px 등)는 외우지 않는다. 기억나지 않는다고 답해도 된다. 아래는 전부 찾아보는 자료다.

---

## 상황
HTML/CSS와 Bootstrap으로 프로필 페이지를 만든 뒤, 완성된 그 페이지에 5가지 변경 요구사항이 주어졌다.

| # | 요구사항 | 내용 |
|---|---|---|
| 1 | 페이지 폭 제한 | 콘텐츠 최대 너비 1000px, 화면 가운데 배치, 위아래 여백 40px |
| 2 | 프로필 영역 박스화 | 영역 전체에 `border`, `padding`, `border-radius` 적용 |
| 3 | 버튼 위치 변경 | `[취소] [저장]`을 오른쪽 정렬 → 가운데 정렬 (간격은 유지) |
| 4 | 학습 카드 정렬 | 카드 3개 뭉치 전체를 페이지 가운데로 |
| 5 | hover 효과 변경 | 1.03배 확대 → 위로 6px 이동, 0.3초 |

특히 5번에는 `animation`과 `@keyframes`를 쓰지 않고 `transition` + `:hover`만 사용하라는 제약이 있었다.

## 문제
원하는 화면을 만들기 위해 CSS나 Flexbox를 어느 요소에 적용해야 하는지 판단하는 데 자주 막혔다. 배치하려는 요소가 아니라 다른 요소에 Flexbox를 적용하거나, 꾸밀 범위를 정확히 잡지 못하는 경우가 있었다.

요구사항 자체도 적용 대상을 헷갈리기 쉬운 형태였다.

- **프로필 박스화**: `input`에 `border`를 주는 것이 아니라, 프로필 영역 전체를 감싸는 요소를 꾸며야 했다.
- **카드 정렬**: 각 카드 내부의 텍스트가 아니라, 세 카드 뭉치 자체를 배치해야 했다.

대상을 잘못 잡으면 결과가 완전히 달라지는 요구사항들이었다.

## 행동
property부터 작성하지 않고, 적용할 위치를 먼저 정하는 기준을 세웠다.

> **꾸밀 대상 확인 → 배치할 요소 확인 → 배치 요소들의 직접 부모 확인**

즉 꾸미는 작업이면 그 범위를 감싼 요소에, 배치하는 작업이면 배치할 요소들의 직접 부모에 적용한다.

- 프로필 영역 전체를 꾸밀 때 → 그 영역을 감싼 요소에 CSS를 적용했다.
- 버튼과 카드의 위치를 바꿀 때 → 각각의 직접 부모에 Flexbox 정렬을 적용했다.
- `hover` 효과는 `animation`과 `@keyframes`를 사용하지 않는 조건이 있었기 때문에, 기본 selector에 transition을 두고 :hover에 이동을 작성했다.

이후 기존 페이지와 구조가 다른 주간 학습 페이지(`f3_independent_reproduction.html`)를 만들 때도, 완성 코드를 보지 않고 같은 기준으로 HTML 구조와 Flexbox 적용 위치를 직접 결정했다.

## 결과
5가지 변경 요구사항을 모두 반영했고, 구조가 다른 페이지를 완성 코드 없이 독립 구현했다.

이 페이지에서 Flexbox를 적용한 위치는 네 곳이고, **네 곳 모두 배치할 요소의 직접 부모**다.

| 배치할 대상 | 적용한 부모 | 결과 |
|---|---|---|
| 제목과 링크 묶음 | `<header>` | 좌우 양끝 배치 (`justify-content-between`) |
| box 2개 | 두 box를 감싼 `<section>` | 가운데 배치 (`justify-content-center`) |
| 기록하기 버튼 | 버튼을 감싼 `<div>` | 오른쪽 정렬 (`justify-content-end`) |
| 카드 3개 | 카드를 감싼 `<div>` | 가운데 배치 (`justify-content-center`) |

배치와 카드 컴포넌트는 Bootstrap의 class로 처리하고, `box`나 `bottom-section` 같은 부분은 별도 CSS 파일로 분리했다.

## 배운 점
CSS와 Flexbox는 property를 많이 아는 것만으로 해결되지 않는다. 먼저 어떤 요소를 바꾸려는지와, 그 요소들의 부모·자식 관계를 파악하는 것이 중요하다. 이 기준을 세운 뒤에는 기존 화면 수정뿐 아니라 새로운 화면에서도 적용 위치를 판단할 수 있었다.

---

## 면접용 핵심 문장
HTML/CSS와 Bootstrap 학습 중 완성한 프로필 페이지에 5가지 변경 요구사항을 추가로 적용하는 실습을 했는데, CSS를 적용할 위치를 자주 헷갈렸습니다. 그래서 '꾸밀 대상 → 배치할 요소 → 직접 부모' 순서로 판단하는 기준을 세웠고, 요구사항을 모두 반영했습니다. 이후 구조가 다른 페이지를 완성 코드 없이 구현했는데, Flexbox를 적용한 네 곳이 전부 배치할 요소의 직접 부모였습니다.

## 예상 꼬리질문

**Q. 구체적으로 어떤 요구사항이었나요?**
크게 세 종류였습니다. 꾸미는 쪽으로는 페이지 폭 제한과 프로필 영역 박스화, 배치 쪽으로는 버튼과 카드의 정렬 변경, 그리고 hover 효과 변경이었습니다. 정확한 수치까지는 기억나지 않지만, 폭을 제한해 가운데로 모으고 흩어져 있던 요소들을 정돈하는 방향이었습니다.

**Q. 어떤 부분이 헷갈리기 쉬웠나요?**
프로필 박스화는 `input`에 `border`를 주는 게 아니라 영역 전체를 감싸는 요소를 꾸미는 일이었고, 카드 정렬은 카드 안의 텍스트가 아니라 카드 세 개 뭉치 자체를 배치하는 일이었습니다. 대상을 잘못 잡으면 결과가 완전히 달라졌습니다.

**Q. 그 기준이 실제로 통한다는 건 어떻게 확인했나요?**
구조가 다른 페이지를 완성 코드 없이 다시 만들면서 확인했습니다. header의 좌우 배치, box 2개 가운데 배치, 버튼 오른쪽 정렬, 카드 3개 가운데 배치까지 네 군데에 Flexbox를 썼는데 전부 배치할 요소의 직접 부모였습니다.

**Q. 그 요구사항은 누가 준 건가요?**
혼자 학습하는 중이어서, 제가 만든 페이지에 변경 요구사항을 걸어보는 방식으로 진행했습니다. 정답 코드를 보지 않는 것을 조건으로 두고, 요구사항만 받아서 어느 요소를 수정할지 직접 판단했습니다.


## 2026-08-16 경험 후보 —  CSS 적용 위치 혼동을 판단 기준으로 바꾼 경험

### 한 줄 요약

CSS 문법은 알고 있었지만 **"어느 요소에 적용할지"** 에서 반복해서 막혔고, 브라우저로 직접 비교해 판단 기준을 만든 뒤 다음 구현에 적용했다.

---

### 상황

HTML/CSS와 Flexbox를 학습한 뒤 직접 페이지를 구현했다. 그런데 CSS property의 의미를 알고 있어도, **어느 HTML 요소에 적용해야 하는지** 판단하는 과정에서 반복해서 막혔다.

### 문제

- 카드 여러 개를 배치하면서 `.card` 자체에 `display: flex`를 적용하려 했다.
- 버튼 자체가 변해야 하는 상황에서 부모 요소를 selector로 선택했다.

문법을 모르는 문제라기보다, **요구사항과 HTML 구조를 연결하는 판단**이 부족한 문제라고 판단했다.

### 행동

- 화면에서 구조가 명확하지 않을 때는 요소에 임시로 `border`를 적용해 실제 영역을 확인했다.
- 부모와 자식에 `display: flex`를 번갈아 적용·제거하면서 브라우저 결과를 비교했다.
- 비교한 결과를 다음 기준으로 정리했다.
  - 여러 요소를 **배치**한다 → 그 요소들의 **직접 부모**를 찾는다.
  - 특정 요소 **자체가 변한다** → 그 **요소 자체**를 선택한다.
- 이후에는 CSS를 바로 작성하지 않고, 아래 순서로 먼저 판단했다.

```
요구사항 → 대상 → HTML 구조 → selector → CSS
```

### 결과

- **6일차**: 이전 페이지와 구조가 다른 페이지를 완성 코드 없이 독립 구현하는 데 성공했다.
- **일요일 종합 구현**: 일부 적용 대상 판단 오류가 다시 발생했지만, 문제점을 지적받은 뒤 정답 코드를 옮기지 않고 직접 수정했다.

판단 기준이 완전히 자동화된 단계는 아니다. 다만 반복되는 실수를 단순 암기로 넘기지 않고, **검증 가능한 기준**으로 바꾸어 다음 구현에 적용한 경험이다.

### 활용 가능 질문

- 새로운 기술을 학습할 때 어떻게 이해하는가?
- 반복되는 실수를 줄이기 위해 어떤 방법을 사용했는가?
- 코드가 예상대로 동작하지 않을 때 어떻게 원인을 확인하는가?

### 보완 필요

새로운 요구사항에서도 같은 판단 기준을 안정적으로 적용한 사례가 더 쌓이면 경험의 근거가 강해진다.


## 2026-08-17 경험 후보 — 개별 개념은 알지만 실행 흐름을 연결하지 못한 문제를 구현 단계로 나눠 좁힌 경험

### 상황

JavaScript 기본 문법과 DOM/event 조작을 학습한 뒤, 버튼을 클릭하면 조건을 판단해 화면의 문구를 변경하는 기능을 완성 코드 없이 구현했다.

---

### 문제

- **주 문제** — HTML 요소 찾기와 조건식 작성까지는 직접 했지만, 클릭 event와 실행할 함수를 연결하지 못했고 조건문을 어느 위치에 넣어야 하는지도 판단하지 못했다.
- **부수 문제** — `textContent`를 property가 아니라 method처럼 작성했다.

---

### 확인한 과정

작성한 코드를 처음부터 다시 고치기보다, 어느 단계까지 스스로 구현했는지 나누어 봤다.

- 요소 찾기 → 가능
- 상태값과 조건식 작성 → 가능
- **――― 여기서부터 막힘 ―――**
- event 등록 → 막힘
- event 발생 후 조건 판단과 DOM 변경 연결 → 막힘

경계선은 **event 등록 지점**이었다. 이를 통해 문제를 JavaScript 전체가 아니라 event 이후의 실행 흐름을 연결하는 부분으로 좁혔다.

---

### 보완한 방법

핵심 구조를 안내받은 뒤, `addEventListener()`가 event 발생 시 실행할 함수를 등록한다는 점을 다시 확인하고 조건 판단과 `textContent` 변경을 그 함수 안에 배치했다. 또한 `textContent`가 method가 아닌 property라는 점을 확인해 문자열을 대입하는 방식으로 수정했다.

---

### 결과

- 최초 독립 재현에는 실패했다.
- 안내를 받은 뒤에는 코드 구조를 **요소 찾기 → event 등록 → 조건 판단 → DOM 변경** 순서로 다시 연결할 수 있었다.
- 개별 문법을 아는 것과 여러 개념의 실행 순서를 스스로 구성하는 것은 별개의 문제라는 점을 확인했다.

---

### 배운 점

여러 개념이 섞인 코드에서 막히면 전체를 막연하게 다시 보기보다, 어디까지 스스로 연결했고 어느 단계부터 연결하지 못했는지 나눠서 원인을 좁히는 것이 효과적이었다.

---

### 면접에서 기억할 핵심 (30초 버전)

DOM과 event를 완성 코드 없이 구현하다가, 개별 개념은 이해했지만 실행 흐름을 연결하지 못했다. 코드를 처음부터 다시 보는 대신 구현 단계를 나눠 확인했고, 요소 찾기와 조건식까지는 되지만 event 등록부터 막힌다는 것을 확인했다. 안내를 받은 뒤 조건 판단과 DOM 변경을 event 함수 안에 다시 연결했다. 최초 독립 구현에는 실패했지만, 통합 구현에서 막힌 연결 지점을 먼저 분리해서 찾는 기준을 만들었다.
