1. 파일에서 제목을 찾아 수정하는 탐색이 O(n)인 이유는 무엇인가?
제목을 알지 못해 순차 탐색으로 하나씩 차례대로 비교해야하는데 최악의 경우 파일 안의 모든 줄들을 다 비교해야 하기 때문에

2. memo_lines[index] = changed_line 자체는 왜 O(1)인가?
index 접근을 통해 위치 후보를 선정하고 후보를 통해 위치를 비교하기 때문에
[수정 후]
이미 알고 있는 index를 이용해 리스트의 해당 위치에 바로 접근하고 값을 변경하므로 O(1)이다.

3. 특정 줄 하나만 수정했는데도 전체 파일 수정이 O(n)인 이유는 무엇인가?
수정된 대상을 포함한 모든 대상을 새 리스트에 복사해야하기 때문이다.
[수정 후]
파일 전체를 읽어야하고, 
한 줄을 수정한 뒤에도 
수정된 전체 내용을 파일에 다시 저장해야 하므로 O(n)이다.

4. 삭제 함수에서 대상을 찾은 뒤 break를 사용할 수 없는 이유는 무엇인가?
삭제 대상을 제외한 나머지 모든 대상을 새 리스트에 복사해야하기 때문이다.
[수정 후]
삭제 대상을 찾은 뒤에도
그 뒤에 있는 정상 줄들을
새로운 리스트에 저장해야하므로

5. 오늘 만든 파일 수정 프로그램의 공간 복잡도가 O(n)인 이유는 무엇인가?
파일을 수정한 후 모든 대상을 수정한 공간에 복사하기 때문에
[수정 후]
파일 전체에 파일 복사본을 저장하고, 
삭제 함수에서는 남길 줄들을 새로운 리스트에도 저장하므로 
필요한 추가 공간이 파일 크기에 따라 증가해 O(n)이다.