# 2026-07-15 10일차 공부일지

## [오늘 한 일]
딕셔너리의 생성, 조회, 수정, 추가, 삭제를 연습했다.
딕셔너리와 함수를 사용해 학생 점수 관리 기능을 만들었다.
리스트의 순차 탐색과 딕셔너리의 키 조회를 비교했다.

## [막힌 점]
for반복문으로 딕셔너리 내의 값들을 순차적으로 변환시켜줘야하는데, 딕셔너리 키워드를 써야할 곳에 특정한 딕셔너리 키워드를 써서 모든 반복에 같은 값이 적용됐다.
반복할 때 현재 상품의 key를 사용해야 한다는 것을 놓쳤다.
"노트"라는 key를 직접 적어서 모든 상품에 같은 값이 적용됐다.

## [내가 시도한 것]
items()에서 key와 value가 반복마다 바뀌는 것을 확인했다.
동일한 key를 다른 dictionary 사용해서 동일한 key의 서로 다른 value를 각각 조회했다.

## [알게 된 것]
list에서 in을 사용하면 순차탐색을 한다.
dictionary에서 in을 사용하면 key 조회를 한다.

출력만 하는 함수는 호출결과가 None이므로 출력만 할 것이면, 변수에 저장하지 말고 호출하는 것이 좋다.

## [아직 모르는 것]
딕셔너리가 키를 이용해 값을 빠르게 찾는 내부 원리는
아직 정확히 알지 못한다.

## [내일 할 일]
딕셔너리와 함수를 사용한 프로그램 구조를 다시 확인한다.