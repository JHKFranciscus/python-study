# 1. 가상환경을 사용하는 가장 큰 이유는 무엇이라고 생각하는가?
# 프로젝트에서 여러 사람과 함께 제작하기 위해서
#[수정 후]
#프로젝트마다 필요한 패키지와 버전을 분리해서, 다른 프로젝트나 시스템 Python 환경과 충돌하지 않게 관리하기 위해서이다.

# 2. project_A와 project_B가 서로 다른 가상환경을 사용한다면 A에 설치한 외부 패키지가 B에도 자동으로 설치될 것 같은가?
# 서로 다른 가상환경에서는 서로 다른 Python 실행환경과 package를 사용하기 때문에 자동으로 설치되지 않는다.

# 3. python3 -m venv .venv에서 마지막의 .venv는 무엇을 나타낼 것 같은가?
# .venv/라는 폴더를 생성한다는 의미
#[수정 후]
#생성할 가상환경 디렉터리의 이름을 .venv로 지정한 것이다.

# 4. 가상환경은 새로운 운영체제나 가상 컴퓨터를 만드는 것인가?
# 가상환경용 폴더를 생성할 뿐 새로운 운영체제나 가상 컴퓨터를 만들지는 않는다.


# 1. source .venv/bin/activate를 실행하면 터미널에서 무엇이 달라질 것 같은가?
# 터미널에서 행하는 행위가 전부 가상환경에서 행하게 된다.
#[수정 후]
#터미널 프롬프트 앞에 (.venv)가 표시되고, python, pip 등의 명령을 사용할 때 .venv 안의 실행 파일이 우선 사용되도록 바뀐다.
# 2. 활성화 후 which python의 결과는 시스템 Python 경로일까, .venv 안의 Python 경로일까?
# .venv 안의 Python 경로이다.
# 3. 활성화 후 which pip의 결과는 어디를 가리킬 것 같은가?
# 가상환경의 pip파일을 가리킨다.
# 4. 가상환경을 활성화한다는 것은 .venv 안의 Python을 터미널에서 우선 사용하도록 만드는 것인가?
# 가상환경을 환성화한다는 것은 .venv 안의 Python을 터미널에서 우선 사용하도록 만드는 것이다.


# 1. 현재 pip로 패키지를 설치하면 시스템 Python 쪽에 설치될까, .venv 쪽에 설치될까?
# .venv 쪽
# 2. python -m pip에서 python은 지금 어느 Python을 뜻하는가?
# .venv 쪽 python
# 3. python -m pip와 그냥 pip는 현재 활성화된 환경에서는 같은 가상환경의 pip를 사용할 것 같은가?
# 가상환경이 활성화되어있으므로 같은 가상환경의 pip를 사용할 것이다.
# 4. 가상환경 A에 패키지를 설치한 뒤 가상환경 B로 바꾸면 그 패키지를 자동으로 사용할 수 있을까?
# 어떤 가상환경이 존재할 때 그 가상환경에서는 다른 가상환경에 설치된 패키지를 자동으로 사용할 수 없다.


# 1. requests는 시스템 Python과 .venv 중 어디에 설치될 것 같은가?
# .venv
# 2. 설치 후 pip list에는 requests가 나타날 것 같은가?
# yes
# 3. requests를 설치하면서 requests가 필요로 하는 다른 패키지도 함께 설치될 가능성이 있을 것 같은가?
# requests에 내장이 되어있다면 같이 설치되겠지만 그렇지않다면 설치되지 않는다.
#[확인 후]
#resquest가 필요로 하는 다른 패키지들이 의존성으로 지정되어 있어서 pip가 requests와 함께 필요한 패키지들도 설치한다. 
# 4. 지금 .venv를 삭제한다면 이 가상환경에 설치한 requests도 함께 없어질 것 같은가?
# requests는 .venv에 설치한 것이기에 이 가상환경에 설치한 requests도 함께 없어질 것이다.



# 1. pip freeze는 무엇을 출력할 것 같은가?
# 현재 가상환경에 설치된 패키지들을 출력할 수 있다.
# 2. pip freeze > requirements.txt에서 > 는 어떤 역할을 할 것 같은가?
# 좌측을 실행했을 때 나오는 출력을 우측에 저장한다.
# 3. requirements.txt에는 Python 코드가 들어갈까, 패키지 이름과 버전 정보가 들어갈까?
# 패키지 이름과 버전 정보가 들어간다.
# 4. 다른 사람이 이 프로젝트를 받아서 python -m pip install -r requirements.txt를 실행하면 무엇이 일어날 것 같은가?
# requirement.txt 목록에 적혀있는 파일들을 전부 install한다.
# 5. 왜 .venv 폴더 자체보다 requirements.txt 같은 파일로 필요한 패키지를 기록해 두는 것이 유용할 것 같은가?
# .venv 폴더를 한명 한명에게 일일히 전달할 수 없어, 어딘가에 올려두면 거기에서 다운로드를 받으면 되기 때문이다.
#[수정 후]
#.venv 자체를 복사해서 전달하기보다 필요한 패키지와 버전을 requirements.txt에 기록해 두면, 다른 사람이 자신의 환경에서 새 가상환경을 만들고 같은 패키지들을 다시 설치할 수 있기 때문이다.


# 1. deactivate 후 터미널 앞의 (.venv)는 어떻게 될 것 같은가?
# 사라진다.
# 2. deactivate 후 which python은 .venv/bin/python을 계속 가리킬까, 아니면 가상환경 밖의 Python을 가리킬까?
# 가상환경 밖의 Python을 가리킨다.
#[확인 후]
#deactivate 후에는 .venv/bin/python을 더 이상 가리키지 않는다.
#현재 가상환경 밖에는 python 명령이 따로 잡혀 있지 않아 which python은 아무것도 출력하지 않았다.